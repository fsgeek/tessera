# Skeptic read of the capstone registration, full file, 2026-09-17 (after twelve Codex rounds)

Reader: a non-author skeptic step (Claude Opus 5), run by the owner
instance on 2026-09-17 evening against the whole of
`formal/suite/capstone/PREDICTIONS.md` (3504 lines) after Codex round
twelve judged it ready to freeze subject to the author's items. The
file's freeze statement (§8) owes this read: the skeptic log inside the
file is dated 2026-09-15 and predates eleven rounds of repair. Nothing
was run; no model was touched; the reader opened 26 cited `.out` lines
and found every one as claimed.

## Dispositions (owner instance, 2026-09-17)

- **S-1** (C-C15's set (2) still lists seven guarantees its Cases cell
  descends red) — ACCEPTED, defect. Repair on C-C17's pattern: set (1)
  gains the seven for (a)/(b) and set (2) is case-qualified, so the
  Cases lists govern and the builder does not classify a registered
  severing as a broken fixture.
- **S-2** (C-Q6's degraded coverage stated three ways) — ACCEPTED,
  defect. §(5) governs: conjunct 1 honest-key forms and conjuncts 2 and
  3 are asserted in (a), (b) and (d); only conjunct 1's unrestricted
  form is not asserted in (d). Lines 705 and 1436 are brought forward.
- **S-3** ("C-Q6 two conjuncts" in the timebox note) — ACCEPTED, one
  word; the totals are unaffected and stay.
- **S-4** (C-C7 descends the unrestricted authorship red in strict and
  lists it in no set) — ACCEPTED, defect. It goes into set (1) for
  (a)/(b), both configurations, as a descended red.
- **S-5** (status header "Nothing has been run") — ACCEPTED. The header
  now says: no capstone model exists and no capstone query has been
  run; the ~90 family-copy diagnostics are named as what they are.
- **S-6** ("the count stays sixteen" inside C-C15(iii)) — note; a dated
  clause added beside it saying the count paragraph supersedes it.
- **S-7** (the round-12 disposition not entered) — ACCEPTED, mechanical:
  the dated note moves C-C15(i)'s C-Q7 and C-Q8 from predicted to
  descended green in (a)/(b) on `cap25a`/`cap25b`, with the negative
  controls `cap25c`/`cap25d`.
- **S-8**, **S-9**, **S-11**, **S-12**, **S-13** — notes for the new
  skeptic log; S-9 gets one line: the review log gains one bullet per
  Codex round pointing at its record.
- **S-10** (conjunct 3 has one registered event but C-C15(iii) requires
  opposite colours of its two halves) — ACCEPTED as a **clerk decision
  of the owner instance**: the capstone declares **one** registered
  pair judge, `ScopeMisreported`, which is conjunct 3; and **two
  isolation observers**, `ScopeKeyOnly(aid, lyr, kX, kA)` and
  `ScopeIdentityOnly(aid, lyr, id, idA)`, each firing on its own half's
  inequality, declared in §(1) beside the pair judge, **queried as
  observers only in the isolation configurations that name them**
  (C-C15(iii): identity-only reachable, key-only unreachable), never
  counted as queries, never timeboxed, and never a discharge. This is
  the shape the diagnostics used (`ScopeIdentityWrong`,
  `KeyOnlyScopeWrong`), now registered so the builder does not invent
  it. C-C11's and C-C12's cells (S-11) then cite the identity observer
  by its registered name.

**Sequence.** One repair pass (Opus) applying the seven repairs and
appending a "Skeptic log (2026-09-17)" in the house form; then a
thirteenth owner-run Codex pass on the repaired file, because the
twelfth read the file before these edits; then the author's one read.

## The skeptic's report — verbatim

I have read the whole file in order, checked the cited `.out` files, and cross-read the round-12 record and the `ledger-tests-2026-09-14/README.md` batches.

# Skeptic read of `formal/suite/capstone/PREDICTIONS.md` — 2026-09-17

Read against: the whole file start to finish; `formal/suite/ledger-tests-2026-09-14/` (batches 1–16) for every cite I spot-checked; `docs/reviews/…round10/11/12.md`; `LEDGER.md` §6 only where the file's own cites needed resolving. **Nothing was run and no model was touched.** Form follows the 2026-09-15 log: what is claimed / what the text or run shows / severity.

---

## 1. Contradictions inside the file

**S-1. C-C15's set (2) still lists seven guarantees the same row descends RED. — defect.**
`PREDICTIONS.md:2173`, set-(2) cell: *"Not descended, predicted, both configurations: C-Q5, C-Q8, C-Q9; links 1a, 1b, 2a, 2b, 3, 4; `Stripped`, `SignerForged`, `Spliced`, `VersionLied`."* The Cases cell of the **same row** descends seven of those **red** in (a) and (b) for configuration (i): C-Q9 and link 3 (`cap22e_sp7_strict_q6b_q9_inner.out:1334` — verified `RESULT not event(PossessionUnbound(…)) is false`; `cap22f…:1334`); links 1a, 1b, 2a, link 5's `mh` half and C-Q5's relation (`cap23d_sp7_strict_q6b_observers_tuple.out:2573`, `:1735`, `:2297`, `:2017`, `:2883` — all verified `is false`); and `SignerForged` (`cap24b_sp7_strict_q6b_member.out:3211` verified `is false`, against baseline `cap24a…:1421` `is true`). The row says three times *"Set (1), set (2) and set (3) memberships are again NOT edited here; what changes is the descent"*. Under §4's own replacement rule (`:1460-1463`) — *"a companion failing outside its set … is a broken fixture to be diagnosed and recorded, never a finding"* — the builder running C-C15(i) in strict must classify these seven as a broken fixture, while this file's evidence calls them severings.
Sharpest sub-case: **C-Q9 and link 3 are in set (2) in every case and in set (1) in none** (round 9 added them to the Cases lists only); links 1a/1b/2a/5-`mh`/C-Q5 and `SignerForged` were added to set (1) **for (d) only** (rounds 10, 11) and left in set (2) for "both configurations". The file already has the right pattern: C-C17 moved `PossessionTransplanted` **into set (1)** for exactly this reason (`:2336`, *"`PossessionTransplanted` is **NOT** in this set — it is in set (1), as the run requires"*), and C-C6/C-C8 carry per-case wording (*"their strict green is a registered per-case outcome"*). C-C15 alone was left half-repaired. Repair is mechanical: case-qualify set (2) and give set (1) the (a)/(b) members, or say in the cell that set (2) is read per case with the Cases lists governing.

**S-2. C-Q6's degraded coverage is stated three incompatible ways. — defect.**
`:705`: *"Strict (a) and (b); degraded (d) for the entitled-key conjunct only"*. `:1436`: *"C-Q6 in (a) and (b), with the entitled-key conjunct also in (d)"*. But §(5) registers **conjunct 1's honest-key forms green in (a), (b) and (d)** (`:986-987`), **conjunct 2 unreachable in (a), (b) and (d)** (`:998-999`), and **conjunct 3 unreachable in (a), (b) and (d)** (`:1040`, *"the same mode coverage as conjunct 2"*). C-C11's Cases cell agrees with §(5) and not with the header: *"in (d) C-Q6's standing conjunct is asserted only in its **honest-key** forms (§3, §(5))"* (`:1600`). The round-3 repair added conjunct 3 with (d) coverage and the round-5/6 repairs wrote the companion cells against §(5); the header sentence and the timebox note were never brought forward. Only the **unrestricted** form of conjunct 1 is not asserted in (d).

**S-3. §3's timebox note still says C-Q6 is "two conjuncts". — defect (one word).**
`:1439`: *"C-Q3 is per-role …, C-Q4 carries two judges, **C-Q6 two conjuncts**, C-Q5w two set sizes and link 2b three correspondence conjuncts"*. C-Q6 has had **three** conjuncts since the round-3 repair (`:734-736`, `:976`). The round-3 repair pointed at this very paragraph (`:1188-1190`) and left the count. The arithmetic is unaffected — C-Q6 is one timeboxed entry either way, and 7+2+4+3+1+2+7 = 26, 315+120+180+180+90+60+210 = 1155, 4 × 1155 = 4620 all check — so this is a stale gloss, not a broken total. The same stale count sits at `:1987`, which is inside the dated skeptic log and is correctly left as history.

**S-4. C-C7 descends the unrestricted `Accept ⟹ IssuerSigned` red in strict, and it is in neither set (1) nor set (2). — defect.**
`:1571`. Set (1) reads *"**C-Q8** and **link 4**, in both configurations; in (i) also `AcceptedUnderHonestKey ⟹ IssuerSigned`."* The Cases cell then lists as **descended red** in (a): *"the unrestricted `AcceptS ⟹ IssuerSigned` (`:1243`)"* for (i) and *"the unrestricted form (`:1284`)"* for (ii). Verified: `cap17b_sp1_strict_cc7i.out:1243` and `cap17c_sp1_strict_cc7ii.out:1284` are both `RESULT event(AcceptS(…)) ==> event(IssuerSigned(…)) is false`, against the green baseline `cap17a…:555`. The retained-query table registers that query green in (a)/(b) (`:1400`). The builder's-note escape at `:1581-1585` does not cover it: that note is about **degraded** runs where the baseline is already red, and here the strict baseline is green, so the red **is** a mutation effect. Add it to set (1) for (a)/(b), or say explicitly that it is a registered consequence.

**S-5. The status header says "Nothing has been run." — defect (one clause).**
`:5`. Since 2026-09-16 the file cites roughly ninety ProVerif runs made *for* it (`cap1`–`cap24`, thirteen dated "Scratch runs made for this repair" tables). Every one of those sections is scrupulous — *"copies of a committed family model … no `.out` below is a capstone result"* — but the header the author reads first is unqualified, and the file's own round-7 rule (`:1505-1520` preamble) is precisely that an unqualified all-clear claim is what goes wrong. The 2026-09-15 review log's *"no scratch run was made for this file"* (`:1912`) was true when written and is correctly dated; the header is not dated. Suggested: *"No capstone model exists; no capstone query has been run."*

**S-6. "the count stays sixteen" survives inside C-C15(iii). — note.**
`:2173`, in the round-4 parenthetical registering configuration (iii). §4's count paragraph (`:1631-1640`) supersedes every earlier "sixteen" *"outside the skeptic log and outside the dated repair sections"*; this one carries a dated repair marker but sits in the operative §9(1) table rather than in a `## Repairs after…` section, so whether it is superseded is a reading. It is the only surviving "sixteen" outside `:1634-1636`. Record, no repair.

---

## 2. Claims stronger than their evidence

**S-7. The round-12 disposition is not in the file. — defect (mechanical).**
`docs/reviews/…round12.md` disposition 1: the four control runs *"let two of configuration (i)'s **predicted** greens become **descended** greens; that is a strengthening, **entered in the file as a dated note**, not a repair."* `ledger-tests-2026-09-14/README.md`'s sixteenth batch records `cap25a`–`cap25d` with the C-Q7 and C-Q8 observers green at `cap25a:1306`, `:1317` and their negative controls red at `cap25c:2249`, `cap25d:2505`. **`PREDICTIONS.md` contains no occurrence of `cap25` and no round-12 section** (last repair heading `:3397`, file ends `:3504`; file mtime 20:52, review ran 20:53–21:03). Nothing in the file is *false* as a result — C-Q7 and C-Q8 remain correctly labelled "predicted green" for C-C15(i) — but the last non-author round's only disposition is unentered, and the freeze statement requires that pass to be complete.

**S-8. Evidence discipline otherwise holds where I checked it.** — note.
I opened 26 cited `.out` lines across `cap8a/b`, `cap9`, `cap11a/c`, `cap13a/b`, `cap17a/b/c`, `cap20d/f/g`, `cap22d/e/g`, `cap23d`, `cap24a/b`, `cap16a`, and the committed `base_sp1q1`, `base_sp2`, `base_sp7`, `ss_q1_strict_dns_compromised`. **Every one reads as claimed, with the polarity claimed.** I found no "descended" label over a run that does not declare the query — the round-7 rule (`:1505-1520`) appears to have been applied consistently, including the two cells it names (C-C3, C-C10's isolation control) and the honest self-correction at `:2173` that `cap23g`/`cap23h` are degraded despite `strict` in their filenames. No probability lost its basis: the four repairs that touched a basis sentence (`:1400` round 3 finding 4; `:327-345` round 7; `:1176-1180` round 3; `:1243` round 9) each restate the prediction as unchanged and say why.

**S-9. The Review log records one round of twelve. — note.**
`:1901-1915` carries only the 2026-09-15 entry. The eleven repair sections are each dated and self-describing, so nothing is hidden, but a reader looking for "what review has this been through" finds one bullet. Record, no repair.

---

## 3. Things a builder cannot build from this file alone

**S-10. Conjunct 3 has one registered event but two required halves. — defect (build).**
§(1) declares exactly one judge event: `ScopeMisreported(bitstring, bitstring, pkey, bitstring, pkey, bitstring)` over `(aid, lyr, kX, id, kA, idA)` (`:748-757`), and §(5) fires it *"when `(kA, idA) ≠ (kX, id)`"* (`:1039-1040`), closing with *"What changes is the **arity of the comparison**"* (`:1106`). But C-C15(iii) puts **conjunct 3's identity half in set (1)** (*"reachable with `kA = kX` and `idA = idW ≠ idI = id`"*) and **conjunct 3's key half in set (2)** (*"Set (2), must stay green — and this is the point of the configuration: it includes CONJUNCT 3's KEY HALF. A judge comparing the **key alone** is **unreachable** here"*), both at `:2173`. Under the single pair-form judge those are the **same query**, required red and green in one configuration. The diagnostics that showed the split used two extra events, `ScopeIdentityWrong` and `KeyOnlyScopeWrong` (`:2642`, `:2643`), which exist in the scratch copies only. The builder must be told whether the capstone declares one pair judge or three (pair + key-only + identity-only); the sets as written require three.

**S-11. C-C11's and C-C12's three lists descend a capstone green on a non-capstone event. — note.**
`:1600` (*"conjunct 3 in both halves, `ScopeMisreported` (`:1026`) and `ScopeIdentityWrong` (`:1033`)"*) and `:1601` (*"conjunct 3 in both halves (`:813`, `:821`)"*). `ScopeIdentityWrong` is declared in no capstone registration; it is the diagnostic's second judge. Once S-10 is settled these cells resolve either way, but as written they name a green on a query the capstone does not have.

**S-12. `ScopeReported` is the one declared event given no ProVerif signature. — note.**
`:246` gives it as a table row (*"`aid`, `lyr`, `kA`, `idA`"*) where `LayerAccepted`, `EstablishedWrapped`, `StandingUnentitledWrapped`, `ScopeMisreported` and `HonestWrappedStandingAgreed` all carry explicit type lists. The types are inferable from `ScopeMisreported`'s (`bitstring, bitstring, pkey, bitstring`). Record, no repair.

---

## 4. Provenance labels

**S-13. The four contestable choices and the routed fork read correctly. — no finding.**
Checked each against the required form. Same-key wrapping: *"**this file's fixture choice, contestable by the author at the freeze commit**; it is not the ledger's and is not an author ruling"* (`:67`). Compromised-channel-only enrolment: *"**this file's clerk reading, contestable by the author at the freeze commit**"* (`:65`). `ev` as the pair in strict: *"a clerk disposition of the owner instance, open to the author at the freeze commit"* (`:187`, restated `:327`). The report contract: *"**this file's contract term, CONTESTABLE by the author at the freeze commit; the FOURTH such choice**"* (`:202`). All four are gathered again in §8 (`:1846`, *"clerk dispositions of the owner instance, contestable by the author at the freeze commit without an amendment … they are not author rulings and are not routed"*) with items 3 and 4 marked **contestable** in place (`:1875`, `:1888`). The `REFUSAL` fork states both options and both costs and closes *"still the only item routed to the author, and it is still unresolved here"* (`:1898-1899`); §8's freeze statement requires it answered before the commit (`:1796`). The model-path divergence is correctly *"offered for veto, not routed"* (`:1824`). Every ADDITION beyond §6 I could find carries its marker: the per-link query split (`:300`), the adversary key and lineage (`:65`, `:69`), the standing path (`:70`), C-Q6's modes (`:711`), conjunct 3 (`:1019-1022`). **No sentence in this file reads as an author ruling**, and the only block quote is A3 §A3.3, which the 2026-09-15 log verified verbatim.

---

## 5. What the author must read to freeze responsibly

Four passages, ~2,300 words, **10–12 minutes**; the fifth is optional and adds ~8.

| # | Lines | What it is | Words |
|---|---|---|---|
| 1 | `:1777-1798` | §8 freeze statement — what the commit does and what is owed before it | 200 |
| 2 | `:1799-1900` | §8 routed to the author — the `REFUSAL` fork with both costs (his one decision), the model-path veto, and the four contestable choices listed in one place | 893 |
| 3 | `:65` and `:67` | §1.1's Adversary and Wrapper rows — contestable choices 1 and 2 in their operative form, each with the run that shows the alternative changes a colour | 621 |
| 4 | `:176-241` | §1.3's `ev`-pair and report-contract paragraphs — contestable choices 3 and 4, and the `LayerAccepted` / `ScopeReported` event table | 547 |
| 5 (optional) | `:1670-1702` | §5's thirteen-row matrix — what the whole ladder buys if every prediction holds | 1,126 |

Passages 2–4 are the whole of what is contestable; nothing else in the file needs his ruling. The reviewer's own answer at round 12 was the same list, in the same order.

---

## Verdict

**Not ready for the author's freeze read.** Six repairs are owed, none of them touching a query, a probability, a timebox or the routed fork, and all six mechanical:

- **S-1** — C-C15's set (2) lists seven guarantees the same row descends red; case-qualify set (2) or move them into set (1), on the pattern C-C17 already uses.
- **S-2** — C-Q6's degraded coverage at `:705` and `:1436` contradicts §(5) and the C-C11/C-C12 cells; bring both forward.
- **S-3** — "C-Q6 two conjuncts" at `:1439` is three.
- **S-4** — C-C7's strict-descended `Accept ⟹ IssuerSigned` red belongs in a set.
- **S-5** — "Nothing has been run" at `:5` needs its one qualifying word.
- **S-7** — the round-12 disposition's dated note (`cap25a`/`cap25b`) is not in the file, so the twelfth non-author pass is not closed.

**S-10** is a build decision the file must make before the builder does — one pair judge or three — and I flag it as a defect rather than a build item only because C-C15(iii)'s two sets currently require opposite colours of one registered query. S-6, S-8, S-9, S-11, S-12 and S-13 are for the log: no repair. Evidence discipline across the ~90 archived runs is, on every cite I opened, exactly what the file claims, and the provenance labels are clean — the four contestable choices read as contestable, the `REFUSAL` fork reads as routed and open, and nothing reads as an author ruling.
