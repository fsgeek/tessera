# Codex non-author review of the capstone registration, thirteenth round (2026-09-17) — post-skeptic confirmation

Reviewer: OpenAI Codex CLI (`gpt-6-astra`), run non-interactively by the
owner instance on 2026-09-17 (21:26–21:34 UTC) against the whole of
`formal/suite/capstone/PREDICTIONS.md` after the seven skeptic-read
repairs; scratch diagnostics under `/tmp/tessera-capstone-r13-w3lu9otg/`.
**The reviewer found three bounded corrections, all introduced by the
skeptic repair**, and no new query escape; it confirms the two isolation
observers are declared with consistent arities and behave as registered
on family copies, that the timebox totals still add, that the `cap25`
note is exact (it re-ran `cap25a`–`cap25d`, every result matching), and
that the `REFUSAL` fork and the contestable choices are unchanged. Every
run it cites was reproduced on our tree
(`formal/suite/ledger-tests-2026-09-14/cap26a`–`cap26d`, seventeenth
batch). No capstone model exists; nothing in the repository was run.

## Dispositions (owner instance, 2026-09-17)

All three are **clerk dispositions**; all three are one-sentence
corrections of the skeptic repair.

1. **S-10's observer paragraph narrows D11's consumer to conjunct 3
   "and nothing else".** ACCEPTED. D11 has two consumers: the
   unconditional C-Q1 link 6b and C-Q6's conjunct 3 for its conditional
   half (`:556-567`, `:1776`). The sentence at `:841-844` is qualified
   to "D11's C-Q6 half"; the withdrawn phrase quoted.

2. **S-1's repair carried link 1b into C-C15(i)'s degraded set (1) and
   set (2).** ACCEPTED. C-Q1-degraded does not assert link 1b
   (`:307-311`, `:362-371`: strict only), and the reviewer shows why it
   cannot: adding the publication correspondence to the **unmutated**
   degraded S-P3 model makes it `is false`
   (`cap26a_sp3_degraded_publication_baseline.out:676`) — a degraded
   baseline cost, not a severing. Link 1b comes out of case (d)'s
   companion sets in C-C15(i) (and anywhere else a (d) set names it);
   its strict outcomes stand.

3. **S-4's repair labelled C-C7's case (b) "descended".** ACCEPTED.
   `cap17b`/`cap17c` leak `skD` only; case (b) is predicted on channel
   symmetry, as the same row's Cases cell already says. Set (1) and the
   2026-09-17 skeptic log entry say "(a) descended, (b) predicted".

4. **The observers, confirmed.** The reviewer transcribed
   `ScopeKeyOnly`/`ScopeIdentityOnly` onto the pair-judge copies:
   correct control all three unreachable
   (`cap26b_ss_q1_pairjudge_correct_observers.out:576`, `:583`, `:590`);
   identity-only mutation fires the pair judge and the identity observer
   only (`cap26c…out:959`, `:1334`; key observer green `:1341`);
   key-only mutation fires the pair judge and the key observer only
   (`cap26d…out:955`, `:1340`; identity observer green `:963`);
   witnesses reachable in all three. Entered as the observers' descent
   at the projection.

**Loop.** One repair pass (three sentences), then a fourteenth
owner-run pass to confirm; then the author's read.

## Codex review — verbatim

## 1. Query contracts and discharge

No new query escape demonstrated. One discharge-description contradiction remains:

**1. S-10 incorrectly names conjunct 3 as D11’s only consumer.** The new observer paragraph says D11’s consumer “is conjunct 3 and nothing else.” But D11 also requires **unconditional C-Q1 link 6b**; the file explicitly explains why conditional conjunct 3 cannot replace it. Qualify the new sentence as referring to **D11’s C-Q6 half**. The query itself remains correctly registered.  
Citations: `formal/suite/capstone/PREDICTIONS.md:841–844`, `:556–567`, `:1776`.

## 2. Companion consistency

**2. C-C15(i) assigns a degraded failure to an excluded query.** Its set (1) explicitly predicts **link 1b red in (d)**, and the repaired set (2) repeats that assignment. C-Q1-degraded explicitly excludes link 1b: it is strict-only. This is an operative contradiction, preserved by S-1’s repair.  
Citations: `formal/suite/capstone/PREDICTIONS.md:2280`, `:307–311`, `:362–371`.

Moreover, adding publication correspondence to a copy of the **unmutated** degraded S-P3 model produces `is false`. Thus this red would be a degraded baseline cost, not evidence of C-C15’s severing. Remove link 1b from case (d)’s companion sets; preserve its strict outcomes.  
Diagnostic: `/tmp/tessera-capstone-r13-w3lu9otg/degraded_publication_baseline.pv:120–121`; result: corresponding `.out:676`.

Otherwise, C-C15(i)’s strict failed and retained sets now agree with its Cases lists, including the dated cap25 qualification.

## 3. Predictions and cited evidence

**3. S-4 upgrades an unrun case from predicted to descended.** C-C7’s repaired set (1) calls unrestricted authorship **descended red in both (a) and (b)**. Its Cases cell correctly says every case-(b) entry is **predicted from channel symmetry, never a second run**. The new skeptic log repeats the erroneous descent label. Keep the red prediction, but distinguish observed case (a) from predicted case (b).  
Citations: `formal/suite/capstone/PREDICTIONS.md:1655`, `:3752–3757`.

The cap25 note accurately cites its greens and negative controls. I reran cap25a–cap25d from `/tmp`; every `RESULT` line matched the archive. The explicit cap-diagnostic filename/line references checked resolve correctly; the non-result target is an intentional trace citation. No additional contradicted probability found.

## 4. Builder completeness

The scope repair is buildable and internally consistent:

- `ScopeMisreported`: six arguments.
- `ScopeKeyOnly` and `ScopeIdentityOnly`: four arguments each.
- C-C15(iii): pair judge and identity observer reachable; key observer unreachable.

Citations: `formal/suite/capstone/PREDICTIONS.md:797–833`, `:2280`.

I transcribed those observer signatures onto `/tmp` copies of cap15a–cap15c. The correct control kept all three unreachable; identity-only and key-only mutations each fired the pair judge and exactly the corresponding observer. These are family-copy checks, not capstone evidence.

Timeboxes still add: **26 entries, 1,155 minutes per case, 4,620-minute upper bound**. Observers consume existing boxes.  
Citations: `formal/suite/capstone/PREDICTIONS.md:835–843`, `:1483–1527`.

## 5. Routed and contestable choices

The `REFUSAL` fork remains genuine: both options, their costs, and the prohibition on builder selection are explicit. No additional hidden fork demonstrated.  
Citation: `formal/suite/capstone/PREDICTIONS.md:1885–1906`.

Same-key wrapping and compromised-channel-only enrolment remain explicit, contestable fixture choices. I found no new inconsistency requiring either choice to be reversed.  
Citation: `formal/suite/capstone/PREDICTIONS.md:1958–1980`.

## 6. Carried-forward repairs

All seven skeptic repairs are present, but S-1, S-4 and S-10 retain or introduce the contradictions above. The status qualification, C-Q6 degraded coverage, cap25 note and timebox correction check out. The three carried-forward ledger corrections remain operative: unrestricted structural byte binding, publication as correspondence, and fresh intake identity with layer-specific completeness. No further regression in the dispositioned Codex findings was demonstrated.

**This registration is not ready to freeze unchanged.** The author should see first **finding 2**, C-C15’s excluded degraded query; **finding 3**, C-C7’s unsupported case-(b) descent; and **finding 1**, the narrowing of D11’s consumers. These need bounded corrections; the `REFUSAL` ruling remains the author’s. No repository file was changed and no capstone model was written or run.

