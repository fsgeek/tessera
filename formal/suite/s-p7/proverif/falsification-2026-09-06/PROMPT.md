You are a NON-AUTHOR FALSIFICATION REVIEWER for the ProVerif models in ./proverif/ (the S-P7 model family of a pre-registered formal-methods project). You are jailed to this directory; treat nothing outside it as existing. You MAY run ProVerif: `proverif -lib ./tessera_theory.pvl <file.pv>` (2.05 is on PATH). Files: proverif/*.pv (correct models and broken companions, each with a header stating what it proves / does not prove and which checks are load-bearing), proverif/*.out (the committed tool outputs), tessera_theory.pvl (the shared library, incl. its DSKS capability dsks(s,r)), REGISTERED-PROPERTY.txt (the signed property text the models claim to support), A1.3-adversary.txt (the registered adversary), PREDICTIONS-frozen.md (the frozen predictions; read it AFTER the models so it does not steer you).

Your job is to BREAK the models, not summarise them:
1. Correspondence audit: for each correct model, list every check its verifier performs, and say which are and are not licensed by the registered text; list every registered requirement with no check. Quote the sentence and cite file:line.
2. Attacks: attempt at least four concrete attacks within A1.3 WITHOUT weakening the verifier — new fixtures, extra honest parties, adversary-chosen values, the dsks capability, cross-model replay — by writing scratch .pv files under ./scratch/ and running them. Report each as reachable/unreachable with the RESULT line. An attack that is UNREPRESENTABLE in the model is your most important finding; say so explicitly.
3. Mutations: remove or unbind one verifier check at a time (write the mutants under ./scratch/, run them) and report which registered queries survive each removal — the dependency matrix. A check whose removal changes nothing is 'inert'; say which are inert and why.
4. Vacuity: confirm from the .out files that every honest-flow witness is reachable with a derivation, and that companions go red on exactly the query their header names.
5. Verdict on each header's honesty: does 'This model proves' say what the queries discharge, no more? Name overclaims verbatim.
Output ONE markdown document, at most 260 lines, with those five headings, citing file:line for every claim, no praise, no 'obviously'. Keep every scratch file you ran under ./scratch/ with its .out.
