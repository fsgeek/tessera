--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_anchor.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: anchor; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -265,7 +266,7 @@
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
           let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
-              let anchorProof(=h(tlrSig)) = ap in
+              let ignoredAnchor = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
                   let d = lookup2(aid, lin) in
                     StandingDecide(kT, t, aid, d, terminal)
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_authorityS.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authorityS; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -249,7 +250,7 @@
   in(c, (t: bitstring, ev: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   (* degraded first link: the SOLE evidence binds this exact tuple *)
-  let (=STMT_DIGEST, =h(t)) = checksign(ev, pkS) in
+  
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_authority_digestS.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authority_digestS; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -249,7 +250,7 @@
   in(c, (t: bitstring, ev: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   (* degraded first link: the SOLE evidence binds this exact tuple *)
-  let (=STMT_DIGEST, =h(t)) = checksign(ev, pkS) in
+  let (=STMT_DIGEST, ignoredDigest: bitstring) = checksign(ev, pkS) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_authority_tagS.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -249,7 +250,7 @@
   in(c, (t: bitstring, ev: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   (* degraded first link: the SOLE evidence binds this exact tuple *)
-  let (=STMT_DIGEST, =h(t)) = checksign(ev, pkS) in
+  let (uncheckedAuthTag: bitstring, =h(t)) = checksign(ev, pkS) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_derived_identity.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -222,22 +223,22 @@
 
 (* Terminal decision once the derived identity aid is found in the        *)
 (* lineage with per-entry disposition d.                                  *)
-let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring) =
+let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring, presentedAid: bitstring) =
   if terminal = TERM_REFUSED then
-    ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, aid);          (* S4 *)
+    ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, presentedAid);          (* S4 *)
       out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
   else
     ( let TERM_SHIPPED(shippedId) = terminal in
       ( if shippedId = aid then
           ( if d = DISP_SHIPPED then
-              ( event Established(kT, t, aid);
-                event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
-                out(estCh, (kT, t, aid)) )
+              ( event Established(kT, t, presentedAid);
+                event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, presentedAid);  (* S1 *)
+                out(estCh, (kT, t, presentedAid)) )
             else
               (* carried: well-formedness (SC-3 / probe F1) *)
               event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) )
         else
-          ( event StandingReport(ABSENT, SUPERSEDED, kT, t, aid);         (* S2 *)
+          ( event StandingReport(ABSENT, SUPERSEDED, kT, t, presentedAid);         (* S2 *)
             out(reasonCh, (PATH_S2, SUPERSEDED)) ) )
       else
         event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) ).
@@ -252,7 +253,7 @@
   let (=STMT_DIGEST, =h(t)) = checksign(ev, pkS) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
-  let aid = h(core) in
+  in(c, aid: bitstring);
   if se = noTLR then
     ( event StandingReport(ABSENT, NO_TERMINAL_DISPOSITION_EVIDENCE, kX, t, aid);   (* S3 *)
       out(reasonCh, (PATH_S3, NO_TERMINAL_DISPOSITION_EVIDENCE)) )
@@ -268,7 +269,7 @@
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
                   let d = lookup2(aid, lin) in
-                    StandingDecide(kT, t, aid, d, terminal)
+                    StandingDecide(kT, t, aid, d, terminal, h(core))
                   else
                     event StandingReport(ABSENT, STANDING_EVIDENCE_MISMATCH, kT, t, aid) )
               else
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_entitled.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: entitled; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -259,7 +260,7 @@
   else
     ( let withTLR(kT, tlrSig, ap) = se in
       (* ENTITLED KEY, inside the standing path: the TLR key is the key the evidenced tuple names *)
-      if fp(kT) = kfpr then
+      if true then
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
           if kT = kX then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_entry_disposition.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: entry_disposition; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -229,7 +230,7 @@
   else
     ( let TERM_SHIPPED(shippedId) = terminal in
       ( if shippedId = aid then
-          ( if d = DISP_SHIPPED then
+          ( if true then
               ( event Established(kT, t, aid);
                 event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
                 out(estCh, (kT, t, aid)) )
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_env_authorityS.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_authorityS; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -284,7 +285,7 @@
 let EnvelopePath(pkS: pkey) =
   in(c, (t: bitstring, ev: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
-  let (=STMT_DIGEST, =h(t)) = checksign(ev, pkS) in
+  
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
   let attemptCore(=t, ppf, sg, decl) = core in
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_env_bytes.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_bytes; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -289,7 +290,7 @@
   if fp(kX) = kfpr then
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
-  let (=BYTES, =fb) = checksign(sg, kX) in
+  
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
   event AcceptS(ev, t, kX, fb).
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_env_core_tuple.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_core_tuple; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -287,7 +288,7 @@
   let (=STMT_DIGEST, =h(t)) = checksign(ev, pkS) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
-  let attemptCore(=t, ppf, sg, decl) = core in
+  let attemptCore(ignoredTuple, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_env_fingerprint.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_fingerprint; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -286,7 +287,7 @@
          core: bitstring, fb: bitstring, se: bitstring));
   let (=STMT_DIGEST, =h(t)) = checksign(ev, pkS) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
-  if fp(kX) = kfpr then
+  
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_env_frame_alg.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_frame_alg; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -290,7 +291,7 @@
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
-  let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
+  let framed(ot, ignoredField, =id, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
   event AcceptS(ev, t, kX, fb).
 
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_env_frame_fp.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_frame_fp; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -290,7 +291,7 @@
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
-  let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
+  let framed(ot, =alg, =id, ignoredField, mh, cv, pl) = fb in
   if mh = h(t) then
   event AcceptS(ev, t, kX, fb).
 
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_env_frame_id.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_frame_id; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -290,7 +291,7 @@
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
-  let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
+  let framed(ot, =alg, ignoredField, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
   event AcceptS(ev, t, kX, fb).
 
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_env_manifest.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_manifest; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -291,7 +292,7 @@
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
-  if mh = h(t) then
+  
   event AcceptS(ev, t, kX, fb).
 
 (* --- judges ------------------------------------------------------------ *)
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_env_possession.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_possession; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -288,7 +289,7 @@
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
   let attemptCore(=t, ppf, sg, decl) = core in
-  let (=POSS, =t) = checksign(ppf, kX) in
+  
   let (=BYTES, =fb) = checksign(sg, kX) in
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_envelope.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: remove envelope process; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -322,5 +323,5 @@
   ( !AuthorityS(skS, m) | !AuthorityS(skS, m2)
   | !ShipIssuer(skH1, issuerId, m)   | !RefuseIssuer(skH1, issuerId, m)
   | !ShipIssuer(skH2, issuerId2, m2) | !RefuseIssuer(skH2, issuerId2, m2)
-  | !StandingPath(pk(skS)) | !EnvelopePath(pk(skS))
+  | !StandingPath(pk(skS))
   | !Judge | !ReasonJudge )
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_key_equal.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: key_equal; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -261,7 +262,7 @@
       (* ENTITLED KEY, inside the standing path: the TLR key is the key the evidenced tuple names *)
       if fp(kT) = kfpr then
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
-          if kT = kX then
+          if true then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
           let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_lookup.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: lookup; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -267,7 +268,7 @@
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
-                  let d = lookup2(aid, lin) in
+                  let d = DISP_SHIPPED in
                     StandingDecide(kT, t, aid, d, terminal)
                   else
                     event StandingReport(ABSENT, STANDING_EVIDENCE_MISMATCH, kT, t, aid) )
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_lookup_identity.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -267,7 +268,8 @@
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
-                  let d = lookup2(aid, lin) in
+                  in(c, lookupId: bitstring);
+                  let d = lookup2(lookupId, lin) in
                     StandingDecide(kT, t, aid, d, terminal)
                   else
                     event StandingReport(ABSENT, STANDING_EVIDENCE_MISMATCH, kT, t, aid) )
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_missing_branch.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: missing_branch; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -253,7 +254,7 @@
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
-  if se = noTLR then
+  if false then
     ( event StandingReport(ABSENT, NO_TERMINAL_DISPOSITION_EVIDENCE, kX, t, aid);   (* S3 *)
       out(reasonCh, (PATH_S3, NO_TERMINAL_DISPOSITION_EVIDENCE)) )
   else
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_refused_branch.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: refused_branch; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -223,7 +224,7 @@
 (* Terminal decision once the derived identity aid is found in the        *)
 (* lineage with per-entry disposition d.                                  *)
 let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring) =
-  if terminal = TERM_REFUSED then
+  if false then
     ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, aid);          (* S4 *)
       out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
   else
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_signature_key.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: signature_key; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -263,7 +264,8 @@
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
           if kT = kX then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
-          let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
+          in(c, uncheckedKey: pkey);
+          let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, uncheckedKey) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_terminal_identity.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: terminal_identity; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -228,7 +229,7 @@
       out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
   else
     ( let TERM_SHIPPED(shippedId) = terminal in
-      ( if shippedId = aid then
+      ( if true then
           ( if d = DISP_SHIPPED then
               ( event Established(kT, t, aid);
                 event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_terminal_predicate.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: all terminal decisions removed; entry disposition check preserved. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -222,25 +223,12 @@
 
 (* Terminal decision once the derived identity aid is found in the        *)
 (* lineage with per-entry disposition d.                                  *)
-let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring) =
-  if terminal = TERM_REFUSED then
-    ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, aid);          (* S4 *)
-      out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
-  else
-    ( let TERM_SHIPPED(shippedId) = terminal in
-      ( if shippedId = aid then
-          ( if d = DISP_SHIPPED then
-              ( event Established(kT, t, aid);
-                event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
-                out(estCh, (kT, t, aid)) )
-            else
-              (* carried: well-formedness (SC-3 / probe F1) *)
-              event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) )
-        else
-          ( event StandingReport(ABSENT, SUPERSEDED, kT, t, aid);         (* S2 *)
-            out(reasonCh, (PATH_S2, SUPERSEDED)) ) )
-      else
-        event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) ).
+let StandingDecide(kT:pkey,t:bitstring,aid:bitstring,d:bitstring,terminal:bitstring) =
+  if d = DISP_SHIPPED then
+    ( event Established(kT,t,aid);
+      event StandingReport(ESTABLISHED,TERMINAL_DISPOSITION_SHOWN,kT,t,aid);
+      out(estCh,(kT,t,aid)) )
+  else event StandingReport(UNVERIFIABLE,STANDING_EVIDENCE_MALFORMED,kT,t,aid).
 
 (* Bundle: (t, ev, kX, core, fb, se). The standing path evaluates the   *)
 (* evidenced tuple and the entitled key ITSELF (ENUMERATION note 4 item 2);  *)
--- proverif/ss_q1d_degraded_compromised.pv
+++ scratch/m_deg_tlr_tag.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: tlr_tag; original header below is not a claim about this mutant. *)
 (* ====================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;       *)
 (* not adopted; the commit is the author's.                               *)
@@ -263,7 +264,7 @@
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
           if kT = kX then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
-          let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
+          let (uncheckedTag: bitstring, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_anchor.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: anchor; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -286,7 +287,7 @@
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
           let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
-              let anchorProof(=h(tlrSig)) = ap in
+              let ignoredAnchor = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
                   let d = lookup2(aid, lin) in
                     StandingDecide(kT, t, aid, d, terminal)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_authorityD.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authorityD; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -269,7 +270,7 @@
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
-  let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
+  
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_authorityR.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authorityR; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -270,7 +271,7 @@
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
-  let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
+  
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_authority_digestD.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authority_digestD; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -269,7 +270,7 @@
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
-  let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
+  let (=STMT_DIGEST, ignoredDigest: bitstring) = checksign(evD, pkD) in
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_authority_digestR.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authority_digestR; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -270,7 +271,7 @@
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
-  let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
+  let (=STMT_DIGEST, ignoredDigest: bitstring) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_authority_tagD.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -269,7 +270,7 @@
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
-  let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
+  let (uncheckedAuthTag: bitstring, =h(t)) = checksign(evD, pkD) in
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_authority_tagR.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -270,7 +271,7 @@
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
-  let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
+  let (uncheckedAuthTag: bitstring, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_derived_identity.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -242,22 +243,22 @@
 
 (* Terminal decision once the derived identity aid is found in the        *)
 (* lineage with per-entry disposition d.                                  *)
-let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring) =
+let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring, presentedAid: bitstring) =
   if terminal = TERM_REFUSED then
-    ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, aid);          (* S4 *)
+    ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, presentedAid);          (* S4 *)
       out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
   else
     ( let TERM_SHIPPED(shippedId) = terminal in
       ( if shippedId = aid then
           ( if d = DISP_SHIPPED then
-              ( event Established(kT, t, aid);
-                event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
-                out(estCh, (kT, t, aid)) )
+              ( event Established(kT, t, presentedAid);
+                event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, presentedAid);  (* S1 *)
+                out(estCh, (kT, t, presentedAid)) )
             else
               (* carried: well-formedness (SC-3 / probe F1) *)
               event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) )
         else
-          ( event StandingReport(ABSENT, SUPERSEDED, kT, t, aid);         (* S2 *)
+          ( event StandingReport(ABSENT, SUPERSEDED, kT, t, presentedAid);         (* S2 *)
             out(reasonCh, (PATH_S2, SUPERSEDED)) ) )
       else
         event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) ).
@@ -273,7 +274,7 @@
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
-  let aid = h(core) in
+  in(c, aid: bitstring);
   if se = noTLR then
     ( event StandingReport(ABSENT, NO_TERMINAL_DISPOSITION_EVIDENCE, kX, t, aid);   (* S3 *)
       out(reasonCh, (PATH_S3, NO_TERMINAL_DISPOSITION_EVIDENCE)) )
@@ -289,7 +290,7 @@
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
                   let d = lookup2(aid, lin) in
-                    StandingDecide(kT, t, aid, d, terminal)
+                    StandingDecide(kT, t, aid, d, terminal, h(core))
                   else
                     event StandingReport(ABSENT, STANDING_EVIDENCE_MISMATCH, kT, t, aid) )
               else
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_entitled.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: entitled; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -280,7 +281,7 @@
   else
     ( let withTLR(kT, tlrSig, ap) = se in
       (* ENTITLED KEY, inside the standing path: the TLR key is the key the evidenced tuple names *)
-      if fp(kT) = kfpr then
+      if true then
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
           if kT = kX then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_entry_disposition.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: entry_disposition; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -249,7 +250,7 @@
   else
     ( let TERM_SHIPPED(shippedId) = terminal in
       ( if shippedId = aid then
-          ( if d = DISP_SHIPPED then
+          ( if true then
               ( event Established(kT, t, aid);
                 event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
                 out(estCh, (kT, t, aid)) )
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_authorityD.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_authorityD; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -305,7 +306,7 @@
 let EnvelopePath(pkD: pkey, pkR: pkey) =
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
-  let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
+  
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_authorityR.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_authorityR; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -306,7 +307,7 @@
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
-  let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
+  
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
   let attemptCore(=t, ppf, sg, decl) = core in
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_bytes.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_bytes; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -311,7 +312,7 @@
   if fp(kX) = kfpr then
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
-  let (=BYTES, =fb) = checksign(sg, kX) in
+  
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
   event Accept(evD, evR, t, kX, fb).
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_core_tuple.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_core_tuple; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -309,7 +310,7 @@
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
-  let attemptCore(=t, ppf, sg, decl) = core in
+  let attemptCore(ignoredTuple, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_fingerprint.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_fingerprint; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -308,7 +309,7 @@
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
-  if fp(kX) = kfpr then
+  
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_frame_alg.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_frame_alg; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -312,7 +313,7 @@
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
-  let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
+  let framed(ot, ignoredField, =id, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
   event Accept(evD, evR, t, kX, fb).
 
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_frame_fp.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_frame_fp; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -312,7 +313,7 @@
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
-  let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
+  let framed(ot, =alg, =id, ignoredField, mh, cv, pl) = fb in
   if mh = h(t) then
   event Accept(evD, evR, t, kX, fb).
 
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_frame_id.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_frame_id; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -312,7 +313,7 @@
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
-  let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
+  let framed(ot, =alg, ignoredField, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
   event Accept(evD, evR, t, kX, fb).
 
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_manifest.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_manifest; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -313,7 +314,7 @@
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
-  if mh = h(t) then
+  
   event Accept(evD, evR, t, kX, fb).
 
 (* --- judges ------------------------------------------------------------ *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_env_possession.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_possession; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -310,7 +311,7 @@
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
   let attemptCore(=t, ppf, sg, decl) = core in
-  let (=POSS, =t) = checksign(ppf, kX) in
+  
   let (=BYTES, =fb) = checksign(sg, kX) in
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_envelope.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: remove envelope process; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -345,5 +346,5 @@
   | !AuthorityRepo(skR, m) | !AuthorityRepo(skR, m2)
   | !ShipIssuer(skH1, issuerId, m)   | !RefuseIssuer(skH1, issuerId, m)
   | !ShipIssuer(skH2, issuerId2, m2) | !RefuseIssuer(skH2, issuerId2, m2)
-  | !StandingPath(pk(skD), pk(skR)) | !EnvelopePath(pk(skD), pk(skR))
+  | !StandingPath(pk(skD), pk(skR))
   | !Judge | !ReasonJudge )
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_key_equal.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: key_equal; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -282,7 +283,7 @@
       (* ENTITLED KEY, inside the standing path: the TLR key is the key the evidenced tuple names *)
       if fp(kT) = kfpr then
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
-          if kT = kX then
+          if true then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
           let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_lookup.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: lookup; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -288,7 +289,7 @@
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
-                  let d = lookup2(aid, lin) in
+                  let d = DISP_SHIPPED in
                     StandingDecide(kT, t, aid, d, terminal)
                   else
                     event StandingReport(ABSENT, STANDING_EVIDENCE_MISMATCH, kT, t, aid) )
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_lookup_identity.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -288,7 +289,8 @@
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
-                  let d = lookup2(aid, lin) in
+                  in(c, lookupId: bitstring);
+                  let d = lookup2(lookupId, lin) in
                     StandingDecide(kT, t, aid, d, terminal)
                   else
                     event StandingReport(ABSENT, STANDING_EVIDENCE_MISMATCH, kT, t, aid) )
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_missing_branch.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: missing_branch; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -274,7 +275,7 @@
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
-  if se = noTLR then
+  if false then
     ( event StandingReport(ABSENT, NO_TERMINAL_DISPOSITION_EVIDENCE, kX, t, aid);   (* S3 *)
       out(reasonCh, (PATH_S3, NO_TERMINAL_DISPOSITION_EVIDENCE)) )
   else
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_refused_branch.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: refused_branch; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -243,7 +244,7 @@
 (* Terminal decision once the derived identity aid is found in the        *)
 (* lineage with per-entry disposition d.                                  *)
 let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring) =
-  if terminal = TERM_REFUSED then
+  if false then
     ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, aid);          (* S4 *)
       out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
   else
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_signature_key.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: signature_key; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -284,7 +285,8 @@
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
           if kT = kX then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
-          let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
+          in(c, uncheckedKey: pkey);
+          let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, uncheckedKey) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_terminal_identity.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: terminal_identity; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -248,7 +249,7 @@
       out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
   else
     ( let TERM_SHIPPED(shippedId) = terminal in
-      ( if shippedId = aid then
+      ( if true then
           ( if d = DISP_SHIPPED then
               ( event Established(kT, t, aid);
                 event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_terminal_predicate.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: all terminal decisions removed; entry disposition check preserved. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -242,25 +243,12 @@
 
 (* Terminal decision once the derived identity aid is found in the        *)
 (* lineage with per-entry disposition d.                                  *)
-let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring) =
-  if terminal = TERM_REFUSED then
-    ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, aid);          (* S4 *)
-      out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
-  else
-    ( let TERM_SHIPPED(shippedId) = terminal in
-      ( if shippedId = aid then
-          ( if d = DISP_SHIPPED then
-              ( event Established(kT, t, aid);
-                event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
-                out(estCh, (kT, t, aid)) )
-            else
-              (* carried: well-formedness (SC-3 / probe F1) *)
-              event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) )
-        else
-          ( event StandingReport(ABSENT, SUPERSEDED, kT, t, aid);         (* S2 *)
-            out(reasonCh, (PATH_S2, SUPERSEDED)) ) )
-      else
-        event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) ).
+let StandingDecide(kT:pkey,t:bitstring,aid:bitstring,d:bitstring,terminal:bitstring) =
+  if d = DISP_SHIPPED then
+    ( event Established(kT,t,aid);
+      event StandingReport(ESTABLISHED,TERMINAL_DISPOSITION_SHOWN,kT,t,aid);
+      out(estCh,(kT,t,aid)) )
+  else event StandingReport(UNVERIFIABLE,STANDING_EVIDENCE_MALFORMED,kT,t,aid).
 
 (* Bundle: (t, evD, evR, kX, core, fb, se). The standing path evaluates the   *)
 (* evidenced tuple and the entitled key ITSELF (ENUMERATION note 4 item 2);  *)
--- proverif/ss_q1_strict_dns_compromised.pv
+++ scratch/m_dns_tlr_tag.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: tlr_tag; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -284,7 +285,7 @@
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
           if kT = kX then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
-          let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
+          let (uncheckedTag: bitstring, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_anchor.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: anchor; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -288,7 +289,7 @@
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
           let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
-              let anchorProof(=h(tlrSig)) = ap in
+              let ignoredAnchor = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
                   let d = lookup2(aid, lin) in
                     StandingDecide(kT, t, aid, d, terminal)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_authorityD.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authorityD; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -271,7 +272,7 @@
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
-  let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
+  
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_authorityR.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authorityR; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -272,7 +273,7 @@
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
-  let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
+  
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_authority_digestD.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authority_digestD; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -271,7 +272,7 @@
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
-  let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
+  let (=STMT_DIGEST, ignoredDigest: bitstring) = checksign(evD, pkD) in
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_authority_digestR.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: authority_digestR; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -272,7 +273,7 @@
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
-  let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
+  let (=STMT_DIGEST, ignoredDigest: bitstring) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_authority_tagD.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -271,7 +272,7 @@
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
-  let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
+  let (uncheckedAuthTag: bitstring, =h(t)) = checksign(evD, pkD) in
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_authority_tagR.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -272,7 +273,7 @@
          core: bitstring, fb: bitstring, se: bitstring));
   (* strict first link: both channels' evidence binds this exact tuple *)
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
-  let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
+  let (uncheckedAuthTag: bitstring, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_derived_identity.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -244,22 +245,22 @@
 
 (* Terminal decision once the derived identity aid is found in the        *)
 (* lineage with per-entry disposition d.                                  *)
-let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring) =
+let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring, presentedAid: bitstring) =
   if terminal = TERM_REFUSED then
-    ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, aid);          (* S4 *)
+    ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, presentedAid);          (* S4 *)
       out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
   else
     ( let TERM_SHIPPED(shippedId) = terminal in
       ( if shippedId = aid then
           ( if d = DISP_SHIPPED then
-              ( event Established(kT, t, aid);
-                event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
-                out(estCh, (kT, t, aid)) )
+              ( event Established(kT, t, presentedAid);
+                event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, presentedAid);  (* S1 *)
+                out(estCh, (kT, t, presentedAid)) )
             else
               (* carried: well-formedness (SC-3 / probe F1) *)
               event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) )
         else
-          ( event StandingReport(ABSENT, SUPERSEDED, kT, t, aid);         (* S2 *)
+          ( event StandingReport(ABSENT, SUPERSEDED, kT, t, presentedAid);         (* S2 *)
             out(reasonCh, (PATH_S2, SUPERSEDED)) ) )
       else
         event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) ).
@@ -275,7 +276,7 @@
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
-  let aid = h(core) in
+  in(c, aid: bitstring);
   if se = noTLR then
     ( event StandingReport(ABSENT, NO_TERMINAL_DISPOSITION_EVIDENCE, kX, t, aid);   (* S3 *)
       out(reasonCh, (PATH_S3, NO_TERMINAL_DISPOSITION_EVIDENCE)) )
@@ -291,7 +292,7 @@
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
                   let d = lookup2(aid, lin) in
-                    StandingDecide(kT, t, aid, d, terminal)
+                    StandingDecide(kT, t, aid, d, terminal, h(core))
                   else
                     event StandingReport(ABSENT, STANDING_EVIDENCE_MISMATCH, kT, t, aid) )
               else
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_entitled.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: entitled; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -282,7 +283,7 @@
   else
     ( let withTLR(kT, tlrSig, ap) = se in
       (* ENTITLED KEY, inside the standing path: the TLR key is the key the evidenced tuple names *)
-      if fp(kT) = kfpr then
+      if true then
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
           if kT = kX then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_entry_disposition.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: entry_disposition; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -251,7 +252,7 @@
   else
     ( let TERM_SHIPPED(shippedId) = terminal in
       ( if shippedId = aid then
-          ( if d = DISP_SHIPPED then
+          ( if true then
               ( event Established(kT, t, aid);
                 event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
                 out(estCh, (kT, t, aid)) )
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_authorityD.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_authorityD; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -307,7 +308,7 @@
 let EnvelopePath(pkD: pkey, pkR: pkey) =
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
-  let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
+  
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_authorityR.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_authorityR; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -308,7 +309,7 @@
   in(c, (t: bitstring, evD: bitstring, evR: bitstring, kX: pkey,
          core: bitstring, fb: bitstring, se: bitstring));
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
-  let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
+  
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
   let attemptCore(=t, ppf, sg, decl) = core in
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_bytes.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_bytes; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -313,7 +314,7 @@
   if fp(kX) = kfpr then
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
-  let (=BYTES, =fb) = checksign(sg, kX) in
+  
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
   event Accept(evD, evR, t, kX, fb).
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_core_tuple.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_core_tuple; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -311,7 +312,7 @@
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
-  let attemptCore(=t, ppf, sg, decl) = core in
+  let attemptCore(ignoredTuple, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_fingerprint.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_fingerprint; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -310,7 +311,7 @@
   let (=STMT_DIGEST, =h(t)) = checksign(evD, pkD) in
   let (=STMT_DIGEST, =h(t)) = checksign(evR, pkR) in
   let authTuple(id, kfpr, ss, alg, ver) = t in
-  if fp(kX) = kfpr then
+  
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_frame_alg.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_frame_alg; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -314,7 +315,7 @@
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
-  let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
+  let framed(ot, ignoredField, =id, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
   event Accept(evD, evR, t, kX, fb).
 
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_frame_fp.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_frame_fp; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -314,7 +315,7 @@
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
-  let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
+  let framed(ot, =alg, =id, ignoredField, mh, cv, pl) = fb in
   if mh = h(t) then
   event Accept(evD, evR, t, kX, fb).
 
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_frame_id.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_frame_id; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -314,7 +315,7 @@
   let attemptCore(=t, ppf, sg, decl) = core in
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
-  let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
+  let framed(ot, =alg, ignoredField, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
   event Accept(evD, evR, t, kX, fb).
 
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_manifest.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_manifest; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -315,7 +316,7 @@
   let (=POSS, =t) = checksign(ppf, kX) in
   let (=BYTES, =fb) = checksign(sg, kX) in
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
-  if mh = h(t) then
+  
   event Accept(evD, evR, t, kX, fb).
 
 (* --- judges ------------------------------------------------------------ *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_env_possession.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: env_possession; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -312,7 +313,7 @@
   let authTuple(id, kfpr, ss, alg, ver) = t in
   if fp(kX) = kfpr then
   let attemptCore(=t, ppf, sg, decl) = core in
-  let (=POSS, =t) = checksign(ppf, kX) in
+  
   let (=BYTES, =fb) = checksign(sg, kX) in
   let framed(ot, =alg, =id, =fp(kX), mh, cv, pl) = fb in
   if mh = h(t) then
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_envelope.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: remove envelope process; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -347,5 +348,5 @@
   | !AuthorityRepo(skR, m) | !AuthorityRepo(skR, m2)
   | !ShipIssuer(skH1, issuerId, m)   | !RefuseIssuer(skH1, issuerId, m)
   | !ShipIssuer(skH2, issuerId2, m2) | !RefuseIssuer(skH2, issuerId2, m2)
-  | !StandingPath(pk(skD), pk(skR)) | !EnvelopePath(pk(skD), pk(skR))
+  | !StandingPath(pk(skD), pk(skR))
   | !Judge | !ReasonJudge )
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_key_equal.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: key_equal; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -284,7 +285,7 @@
       (* ENTITLED KEY, inside the standing path: the TLR key is the key the evidenced tuple names *)
       if fp(kT) = kfpr then
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
-          if kT = kX then
+          if true then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
           let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_lookup.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: lookup; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -290,7 +291,7 @@
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
-                  let d = lookup2(aid, lin) in
+                  let d = DISP_SHIPPED in
                     StandingDecide(kT, t, aid, d, terminal)
                   else
                     event StandingReport(ABSENT, STANDING_EVIDENCE_MISMATCH, kT, t, aid) )
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_lookup_identity.pv
@@ -1,3 +1,4 @@
+(* REVIEW single-check mutant; original header below is not asserted. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -290,7 +291,8 @@
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
-                  let d = lookup2(aid, lin) in
+                  in(c, lookupId: bitstring);
+                  let d = lookup2(lookupId, lin) in
                     StandingDecide(kT, t, aid, d, terminal)
                   else
                     event StandingReport(ABSENT, STANDING_EVIDENCE_MISMATCH, kT, t, aid) )
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_missing_branch.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: missing_branch; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -276,7 +277,7 @@
   let authTuple(id, kfpr, ss, alg, ver) = t in
   (* identity is DERIVED by the verifier from the core it holds, never read *)
   let aid = h(core) in
-  if se = noTLR then
+  if false then
     ( event StandingReport(ABSENT, NO_TERMINAL_DISPOSITION_EVIDENCE, kX, t, aid);   (* S3 *)
       out(reasonCh, (PATH_S3, NO_TERMINAL_DISPOSITION_EVIDENCE)) )
   else
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_refused_branch.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: refused_branch; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -245,7 +246,7 @@
 (* Terminal decision once the derived identity aid is found in the        *)
 (* lineage with per-entry disposition d.                                  *)
 let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring) =
-  if terminal = TERM_REFUSED then
+  if false then
     ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, aid);          (* S4 *)
       out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
   else
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_signature_key.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: signature_key; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -286,7 +287,8 @@
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
           if kT = kX then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
-          let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
+          in(c, uncheckedKey: pkey);
+          let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, uncheckedKey) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_terminal_identity.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: terminal_identity; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -250,7 +251,7 @@
       out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
   else
     ( let TERM_SHIPPED(shippedId) = terminal in
-      ( if shippedId = aid then
+      ( if true then
           ( if d = DISP_SHIPPED then
               ( event Established(kT, t, aid);
                 event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_terminal_predicate.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: all terminal decisions removed; entry disposition check preserved. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -244,25 +245,12 @@
 
 (* Terminal decision once the derived identity aid is found in the        *)
 (* lineage with per-entry disposition d.                                  *)
-let StandingDecide(kT: pkey, t: bitstring, aid: bitstring, d: bitstring, terminal: bitstring) =
-  if terminal = TERM_REFUSED then
-    ( event StandingReport(ABSENT, ISSUANCE_REFUSED, kT, t, aid);          (* S4 *)
-      out(reasonCh, (PATH_S4, ISSUANCE_REFUSED)) )
-  else
-    ( let TERM_SHIPPED(shippedId) = terminal in
-      ( if shippedId = aid then
-          ( if d = DISP_SHIPPED then
-              ( event Established(kT, t, aid);
-                event StandingReport(ESTABLISHED, TERMINAL_DISPOSITION_SHOWN, kT, t, aid);  (* S1 *)
-                out(estCh, (kT, t, aid)) )
-            else
-              (* carried: well-formedness (SC-3 / probe F1) *)
-              event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) )
-        else
-          ( event StandingReport(ABSENT, SUPERSEDED, kT, t, aid);         (* S2 *)
-            out(reasonCh, (PATH_S2, SUPERSEDED)) ) )
-      else
-        event StandingReport(UNVERIFIABLE, STANDING_EVIDENCE_MALFORMED, kT, t, aid) ).
+let StandingDecide(kT:pkey,t:bitstring,aid:bitstring,d:bitstring,terminal:bitstring) =
+  if d = DISP_SHIPPED then
+    ( event Established(kT,t,aid);
+      event StandingReport(ESTABLISHED,TERMINAL_DISPOSITION_SHOWN,kT,t,aid);
+      out(estCh,(kT,t,aid)) )
+  else event StandingReport(UNVERIFIABLE,STANDING_EVIDENCE_MALFORMED,kT,t,aid).
 
 (* Bundle: (t, evD, evR, kX, core, fb, se). The standing path evaluates the   *)
 (* evidenced tuple and the entitled key ITSELF (ENUMERATION note 4 item 2);  *)
--- proverif/ss_q1_strict_repo_compromised.pv
+++ scratch/m_repo_tlr_tag.pv
@@ -1,3 +1,4 @@
+(* REVIEW MUTANT: tlr_tag; original header below is not a claim about this mutant. *)
 (* ==================================================================== *)
 (* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator;     *)
 (* not adopted; the commit is the author's.                             *)
@@ -286,7 +287,7 @@
         ( (* carried: the TLR key is the key the artifact presents (probe fixture shape) *)
           if kT = kX then
           (* the TLR verifies under the entitled key, under the library tag TLR (D-6) *)
-          let (=TLR, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
+          let (uncheckedTag: bitstring, (lin: bitstring, terminal: bitstring, declT: bitstring)) = checksign(tlrSig, kT) in
             ( (* carried: the anchor proof presented is over the TLR presented (C-2 content) *)
               let anchorProof(=h(tlrSig)) = ap in
                 ( (* the derived identity is looked up in the lineage; absent -> mismatch *)
