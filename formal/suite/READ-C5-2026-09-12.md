# The C5 read — four claim blocks, one question each (assembled 2026-09-12)

**What this is.** ROUTED C5 asks the author to read the claim block of
each of the four new suite models against the registered sentence it
claims to model. This file puts the eight texts side by side so the
read is one sitting. Every block below is copied verbatim by script
from the file and lines named; nothing is paraphrased. **Length:** the
four claim blocks total about 230 lines of comment; the four
registered sentences about 60.

**Three ranges differ from ROUTED C5's, recorded as a divergence, not
absorbed** (skeptic review, 2026-09-12). ROUTED C5 names S-P7 lines
13–75 and S-STANDING lines 20–65; both cut in mid-sentence — S-P7's
inside the "does not prove" list, S-STANDING's inside a quotation in
the LOAD-BEARING list — so the author would have been asked whether a
half-sentence overclaims. They are extended here to the end of the
block each was quoting (13–83 and 20–87); nothing is removed. ROUTED
C5 also names A3 lines 75–85 for P1's operative sentence; 84–85 are
the first two lines of the *next* item (the coverage-map obligation),
so the range is trimmed to 75–83. The quoted text is otherwise
byte-identical to the named files, diffed line for line.

Reading aids exist per family
(`formal/suite/<family>/READING-AIDS.md`) and are optional.

**The one question, per block** (S-P3's, adopted): *Does the claim
block say what that verifier does, no more?* Not: does the model
sufficiently cover the property (that is the coverage map's and the
capstone's business). A "yes" completes the author's header review for
that family. A "no" names the sentence that overclaims or underclaims,
and the header is repaired before anything else moves.

**Answers** are entered at the end of this file, dated, in the author's
words.

---

## 1. S-P1 — integrity (P1)

### Registered sentence — P1's operative form, A3 §A3.1.2 (`docs/phase-0-prereg-amendment-3.md` lines 75–83)

```
2. **The headline precision repair (Sol finding 1).** The H0 headline
   "altering any part requires solving a computationally hard problem"
   is corrected to its operative form, which already coexists in the
   pre-registration (§2, Layer 1): alteration is trivial; **surviving
   verification over altered bytes is what is hard** — no transition
   leads to an accepted receipt over altered bytes. This is a
   claim-correspondence repair, not a construction change or weakening
   under A1.1: the operative form is the checkable claim the models
   already use.
```

### Claim block — `formal/suite/s-p1/proverif/sp1_q2_degraded_compromised.pv` lines 18–73

```
(* This model proves: in degraded mode with the sole authority channel *)
(* compromised, under an adversary that can alter any bytes after      *)
(* issue, holds every key it uses, and has the A1.3 item 3 (DSKS)      *)
(* capability, framed bytes accepted under an honest issuer's key are  *)
(* exactly the bytes that issuer signed at an issuance event — the     *)
(* [model] binding half of P1 in its §A3.1 operative form, as a        *)
(* non-injective correspondence.                                       *)
(* This model does not prove: the verdict partition (P4, TLA+); that   *)
(* acceptance is fresh or context-bound (replay is permitted, §A3.1    *)
(* item 1); that the accepted key is an honest issuer's (P10 / the     *)
(* first link — in degraded-compromised mode it is not, and the        *)
(* unrestricted contrast query (ii) is registered red); re-attribution *)
(* of honest bytes to another key (S-P3, complementary — composed at   *)
(* the capstone, not consumed here); the multi-signer set (S-P2);      *)
(* canonicalization or the frame's byte layout (P8); wrapper soundness *)
(* (P7); anything temporal (P5/P6); the verification profile (H1a).    *)
(* Composition with a same-key signing endpoint outside this fixture   *)
(* (key-use discipline, CORRECTION item 8 below; RESULTS.md ledger     *)
(* entry 5) is not proved either: the claim is over a CLOSED producer  *)
(* set (reviewer's verdict on this header, 2026-09-06).                *)
(* Load-bearing checks (for the headline): the attestation-signature   *)
(* check `let (=BYTES, =fb) = checksign(sg, kX)` binding the PRESENTED *)
(* bytes to the SIGNED bytes, resting on signature unforgeability      *)
(* (library, Dolev-Yao). Confirmed by the S-P1 probes (RESULTS.md,     *)
(* "Load-bearing / carried, from ablation"): only this check, and the  *)
(* unforgeability of sign, move the headline; independently confirmed  *)
(* by the cross-family review's single-removal matrix (2026-09-06,     *)
(* RECORDED item 4 below): every other check is inert for the          *)
(* headline on its own, which does not license removing several        *)
(* together.                                                           *)
(* Carried checks (not exercised by these queries): the fingerprint    *)
(* match and manifest-hash check (S-P3), possession over the manifest  *)
(* (A1.5/P10, S-P3 entry 2), the authority-evidence check (inert in    *)
(* degraded-compromised mode by construction, S-P3 F6), algorithm      *)
(* identifier (A3 identifier-binding invariant, per F7), issuer        *)
(* identity (S-P3 F8 and its 2026-09-05 ruling). Signed fields carried *)
(* as OBLIGATIONS, not checks: object type (P7) and canonicalization   *)
(* version / algorithm profile (P8/H1a) — ot and cv are bound at the   *)
(* frame pattern and never compared (CORRECTION item 7 below).         *)
(*                                                                      *)
(* Theory: formal/suite/lib/tessera_theory.pvl (D-3 possession over    *)
(* manifest; D-4 DSKS expressible; D-5 object-type constants).          *)
(* First link in the SELECTED form: transcription binding, evidence =  *)
(* sign((STMT_DIGEST, h(t)), skCh); the sole channel's key is public.  *)
(* Frame: framed(objType, alg, issuerId, kfp, mh, canonVer, payload)   *)
(* under BYTES — copied VERBATIM from S-P3 (library header: NOT        *)
(* promoted; shared by copy). Object type is the library's OT_ATTEST.  *)
(* Fixture (PREDICTIONS.md "Fixture statement", N1/N2): TWO honest     *)
(* issuers, each with its own honestly published manifest, replicated  *)
(* with adversary-chosen payloads; N1 witness HonestAccepted emitted   *)
(* by the judge, must be reachable.                                     *)
(* Judge encoding as registered (PREDICTIONS.md "The judge"): judge H  *)
(* keyed on honest KEYS (honestKeyCh), judge N1 keyed on honest        *)
(* (key, bytes) reports; report outputs parallel with continuations   *)
(* (S-P3 recut 1); judge locals never shadow library names.            *)
(*                                                                      *)
```

---

## 2. S-P2 — signature-set completeness (P2)

### Registered sentence — P2, Amendment 1 (`docs/phase-0-prereg-amendment-1.md` lines 121–128)

```
- **P2 — Signature-set completeness (anti-stripping).** The canonical
  signed bytes commit to the required-signer manifest. A package presenting
  fewer signatures than its manifest requires yields `INVALID` — never
  `VALID_STRICT`, and `VALID_DEGRADED` only under an explicit, recorded
  policy within the waivable set of A1.2.1. Consequence: "issued with one
  signature" and "issued with two, one stripped" are distinguishable by
  construction, because the expected set is inside what every signature
  signs. **[model]**
```

### Claim block — `formal/suite/s-p2/proverif/sp2_q2_degraded_compromised.pv` lines 14–50

```
(* This model proves: in degraded mode with the SOLE authority channel   *)
(* compromised, under an adversary that holds every key it uses and the DSKS *)
(* capability, (1) no package is accepted for an honest manifest with fewer *)
(* signatures than that manifest's signed required set names [Stripped   *)
(* unreachable]; (2) each required slot is satisfied only by the key the *)
(* signed set names [SignerForged unreachable]; (3) under the fixture rule *)
(* ONE KEY, ONE MANIFEST, no honest key is accepted as a signer of a manifest *)
(* other than the one it signed — note 2's set-shrinking surface [SetAltered *)
(* unreachable; the judge is instrumentation, not a verifier check: see  *)
(* CORRECTION]; and (4) S-P3's key-binding relation is not weakened      *)
(* [Reattributed unreachable, judge carried verbatim] — for required sets of *)
(* at most two signers, read from the map-v1 tuple's kfp and sset fields. The *)
(* honest flow is accepted for both set sizes [HonestComplete reachable for *)
(* M1 and for M2 — existential witnesses, a vacuity guard, not coverage]. *)
(* This model does not prove: anything about required sets larger than two; *)
(* canonical ordering, uniqueness or bounds of the set encoding, or any  *)
(* byte-encoding attack — a coverage failure, not an unreachable attack (P8, *)
(* consumed ledger entry 3; A1.3 item 2's "reorder" has no query here); that *)
(* a stripped package yields INVALID rather than some other non-valid verdict *)
(* (P4, cross-formalism join, consumed entry 4); the VALID_DEGRADED waiver of *)
(* a signature subset (P4's lattice); impersonation by the adversary's own *)
(* key over its own bytes and its own manifest (reachable, the registered *)
(* degraded-mode cost, A4 §A4.6); authorship — a fabricated signature    *)
(* accepted under the honest NAMED key is not detected by any judge here *)
(* (S-P3 F7: S-P1); the verification profile.                            *)
(* Load-bearing checks (confirmed: RESULTS.md F2-F6; cross-family        *)
(* single-removal matrix, see footer): Stripped — the required arity is read *)
(* from the signed tuple's sset PATTERN (=signers0 / signers1(kfprB)), never *)
(* from what the package presents (severed by Q3); SignerForged — the    *)
(* per-slot fingerprint match fp(kX) = kfprX (fp idealization; severed by *)
(* Q4); SetAltered — the frame's manifest hash mh = h(t) (h idealization) and *)
(* possession over the manifest (signature unforgeability), JOINTLY: either *)
(* alone suffices, so each single removal is green (Q5-C1/C3) and only both *)
(* removed is red (Q5-C2).                                               *)
(* Carried, not exercised (d2-d4; footer): the authority-evidence check (sole *)
(* channel key public, S-P3 F6); the S-P3 F7 fields, object type being   *)
(* S-P7's; the attestation-signature check itself (for S-P1).            *)
```

---

## 3. S-P7 — wrapper and object-type soundness (P7)

### Registered sentence — P7, Amendment 1 (`docs/phase-0-prereg-amendment-1.md` lines 235–253)

```
- **P7 — Wrapper and object-type soundness.** Every signed object carries
  a domain-separation type tag *inside the signed bytes*, drawn from the
  enumerated set: base attestation, wrapper, issuer-key manifest,
  authority evidence, conformance vector, review-recency attestation
  (§4.7). No trace exists in which an object of one type is accepted as
  another (no cross-type confusion). A wrapper commits to the inner
  package's **exact bytes, embedded as an opaque byte string** (e.g.,
  base64 of the inner package's framed bytes) — never as parsed JSON.
  Outer canonicalization must be structurally unable to re-serialize,
  re-escape, or otherwise touch the inner byte stream: a nested-JSON
  embedding would let a different outer canonicalization version silently
  alter the inner bytes and break the inner signatures and OTS proof.
  (Hash-only commitment is rejected: the inner bytes must travel *inside*
  the bundle, or P9's self-containment fails.) The wrapper records both
  inner and outer canonicalization versions. Wrapping never alters the
  inner package's
  independently-computed verdict — the wrapper attests the inner *bytes*,
  never the inner *verification result* (preserving §3.1's distinction
  exactly). **[model]**
```

### Claim block — `formal/suite/s-p7/proverif/sp7_q2_degraded_compromised.pv` lines 13–83

```
(* This model proves: in degraded mode with the sole authority channel  *)
(* compromised and the adversary holding an authorized wrapper identity *)
(* (idA: any key it likes, self-signed manifest, channel evidence it    *)
(* mints with the public channel key), a wrapped presentation of an     *)
(* honest attestation is never accepted with the inner artifact         *)
(* attributed to any identity or key other than the inner frame's       *)
(* committed (issuerId, kfp) (C2 — Rescoped unreachable); the honest    *)
(* inner signature term is never ACCEPTED under a key other than its signer's inside a wrapped presentation *)
(* (InnerSigTransplanted unreachable) — CORRECTION 2026-09-06 (cross-family review item 19): first written "never verified", which is literally false: the checksign primitive succeeds under a DSKS-derived key before the frame's =fp(kI) pin rejects (reviewer attack_dsks_checked, falsification-2026-09-06/scratch/: WrongKeyVerified reachable, InnerSigTransplanted unreachable); the claim is about acceptance, which is what the judge reports; *)
(* and no framed object signed as one enumerated type is                *)
(* accepted as another (C1 — TypeConfused unreachable; the model's two acceptance paths: as an attestation, InnerCheck; as a wrapper, VerifierWrapped — no other kind is accepted anywhere) — under a *)
(* perfect fingerprint and hash, deterministic signatures, at nesting   *)
(* depth 1. S-P3's Reattributed judge is transcribed and must stay      *)
(* unreachable on both the base path and the inner object inside the    *)
(* wrapped path (nothing here may weaken S-P3).                         *)
(*                                                                      *)
(* Load-bearing checks for these queries — as ESTABLISHED by ablation  *)
(* (ablations/a01–a18, 2026-09-06; the registered header predicted the =idI / =fp(kI) pair, corrected here before any author read). CORRECTION 2026-09-06 (cross-family review item 4): "load-bearing" below means load-bearing JOINTLY unless a single removal is named red — the reviewer's 198-mutant matrix (falsification-2026-09-06/scratch/guard_groups.tsv, dependencies.tsv) finds every single-check removal green for every query except the frame type equalities (:12 — red on every path in every model except Q4c's inner one, where the canon() shape discriminates), the inner signature check (:9) and, in Q2/Q5 only, its bytes equality (:11). CORRECTION 2026-09-06 (cross-family review item 4, second pass): the parenthetical on :12 narrows this summary, which was first written as a matrix-wide claim and so named a singly-GREEN cell — `q4o_I_type:unchanged` (guard_groups.tsv row 12; dependencies.tsv:117) — as individually red, the overclaim item 4 exists to remove; comment-only, RESULT lines unchanged; *)
(* in particular the =fp(kI) match's single removal is green (a02; matrix :15) — it is load-bearing jointly with the slot and manifest-hash checks, as the a02/a06/a09 record below already says: *)
(*   - Rescoped, KEY half: the inner frame's =fp(kI) match inside InnerCheck alone pins the      *)
(*     attributed key (a17: slot check AND manifest-hash check both removed, =fp(kI) kept: green;  *)
(*     a02 removes =fp(kI): still green, because the slot check fp(kI) = kfprI and the manifest-   *)
(*     hash check mhI = h(tI) together compensate; a06 and a09 remove =fp(kI) with either one of   *)
(*     those: red). The key is pinned to the INNER frame by fp injectivity (S-P3 F5), never read   *)
(*     from the wrapper. S-P3's dependency statement reproduces on the wrapped path for the key.   *)
(*   - Rescoped, IDENTITY half: NOT pinned by =fp(kI). It needs any ONE of the frame's =idI, the   *)
(*     manifest-hash check, or possession-over-manifest under the pinned key (a01, a03, a05: =idI  *)
(*     removed with the other two present, green; a18: all three removed with =fp(kI) kept,       *)
(*     Rescoped RED — honest I2 bytes under pk(skI2) attributed to an adversary-chosen idX). The   *)
(*     header first said "alone suffices"; a17/a18 and this correction: 2026-09-06 skeptic review. *)
(*   - InnerSigTransplanted: the same =fp(kI) route (jointly, as above), PLUS the inner *)
(*     signature check itself (a11: with checksign(sgI, kI) removed the *)
(*     judge fires on an unverified honest signature term presented    *)
(*     beside the adversary's own frame) — the one S-P7 query whose     *)
(*     green depends on the signature check being PRESENT; it does not *)
(*     depend on signature security beyond the registered DSKS theory. *)
(*   - TypeConfused: the =OT_ATTEST equality in InnerCheck and the      *)
(*     =OT_WRAPPER equality in VerifierWrapped (a10 removes the latter: *)
(*     red — an honest attestation whose adversary-chosen payload is a  *)
(*     wrap() term is accepted as a wrapper).                           *)
(* Carried, not load-bearing here (each removed alone leaves every     *)
(* query unchanged): the outer signature and outer fingerprint checks   *)
(* (a13 — the wrapper's authorship is S-P3 entry 1, consumed); inner   *)
(* and outer authority evidence (a12 — inert, sole channel key public, *)
(* S-P3 F6); inner and outer possession over the manifest (a08 — S-P3  *)
(* entry 2); the manifest-hash check alone (a04 — key pinned by =fp(kI), identity by =idI or     *)
(* possession, S-P3 F3); the slot check alone (a07); the                                          *)
(* canonicalization version fields (bound in the frame and recorded in *)
(* wrap(); checked only for presence, never for consistency or support *)
(* — P8/H1a).                                                           *)
(*                                                                      *)
(* This model does not prove: that a concrete opaque embedding is       *)
(* structurally opaque under the outer canonicalization pass (P8/H1a;   *)
(* opacity is true by construction here — wrap() is a transparent term *)
(* and the inner bytes travel unchanged; what CAN be tested is the      *)
(* companion where it is not, Q4); anything at depth >= 2 (Q5);         *)
(* anything about standing evidence or its transplant (S-STANDING, per  *)
(* the A7 ruling of 2026-09-06 — S-P7 models the identity relation      *)
(* only); the P4 verdict partition (rejection here is non-acceptance);  *)
(* that the three unexercised object types OT_MANIFEST, OT_AUTHEVID,    *)
(* OT_CONFVEC — and the two Amendment-4 types OT_TLR, OT_REFUSAL —      *)
(* are distinguishable in bytes (P8 vectors); type confusion involving  *)
(* the manifest (authTuple), possession proof (POSS) or authority       *)
(* evidence (STMT_DIRECT/STMT_DIGEST), which are distinct library      *)
(* constructors with no object-type field — unrepresentable here        *)
(* (ENUMERATION note 1 item 1 / guide M5), byte-level, P8 vectors — the *)
(* open coverage cell; package completeness / self-containment (P7 "the inner bytes must travel inside the bundle", P9): the embedded pair wrap(cvIw, (fbI, sgI)) carries the inner bytes and signature only — the inner manifest, evidence and possession are presenter-supplied outside it (lines 272–273) and no query asks that they travel in the bundle — an open coverage cell, a P8/H1a format obligation, not a symbolic one (cross-family review item 21); byte-encoding attacks generally — non-canonical encodings, hash-input ambiguity, cross-kind parse confusion — are a coverage failure of this algebra, not an unreachable attack (item 2); signature-set operations, A1.3 item 2, are unrepresentable with one signature slot per layer — S-P2's subject (item 1); *)
(* impersonation with the adversary's own key over its own bytes at     *)
(* either level (reachable, the verifier's cost per the 2026-09-05      *)
(* ruling under S-P3 F8 and Amendment 4 §A4.6).                         *)
(*                                                                      *)
```

---

## 4. S-STANDING — protocol standing (A3 §A3.7.1)

### Registered sentence — A3 §A3.7 item 1 (`docs/phase-0-prereg-amendment-3.md` lines 524–548)

```
1. **Standing/equivocation (the A2.0 residue).** The abandoned-anchor
   artifact can leave two cryptographically valid issuance artifacts for
   the same content with different declared times; A2.2 and A2.4 confine
   standing without deduplicating content. Comparative visibility is
   insufficient: a presenter of the abandoned artifact could omit a
   later lineage record. The registered invariant is therefore:

   > Cryptographic validity alone confers no protocol standing. Any
   > artifact claiming standing must present verifiable standing
   > evidence binding its issuance identity, attempt lineage, and
   > terminal disposition. Missing standing evidence leaves the artifact
   > evidentially admissible but without protocol standing.

   A terminal lineage record, capability, transparency witness, or
   another construction may discharge the invariant. The chosen
   mechanism must be testable on an abandoned artifact presented alone,
   not only when competing artifacts happen to meet.

   Standing is an **orthogonal assessment dimension**, not a fifth P4
   state. The base result reports at least `ESTABLISHED`, `ABSENT`, or
   `UNVERIFIABLE`, with evidence and reasons, alongside the unchanged P4
   envelope verdict. Thus an artifact may honestly be
   `verification = VALID_STRICT` and `protocol_standing = ABSENT`.
   Relying-party policy decides whether those facts meet its own
   requirements; Tessera does not collapse them into one checkmark.
```

### Claim block — `formal/suite/s-standing/proverif/ss_q1_strict_dns_compromised.pv` lines 20–87

```
(* This model proves: under the A1.3 adversary with one of two          *)
(* authority channels (DNS) fully compromised, key substitution         *)
(* expressible (D-4), possession free, and any artifact, wrapper-       *)
(* shaped outer artifact, bundle, TLR-shaped object or anchor proof     *)
(* freely constructible, a standing report of ESTABLISHED computed      *)
(* against a key implies that the issuer holding that key signed one    *)
(* TLR whose lineage contains the presented artifact's VERIFIER-DERIVED *)
(* identity h(core) and whose terminal disposition designates that      *)
(* identity — A3.7.1's three bindings under one signature — and that    *)
(* no ESTABLISHED report is computed against a key the evidenced        *)
(* authority tuple does not name. Under a perfect hash (h injective)    *)
(* and a perfect fingerprint (fp injective); the transplant result's    *)
(* entire load is h (plan, "Abstractions").                             *)
(* CORRECTION 2026-09-06 (cross-family review item 24): the             *)
(* unrestricted sentence (i) is FIXTURE-BOUND — it holds because every  *)
(* honestly evidenced tuple in this fixture names an honest key. An     *)
(* honestly ENROLLED adversary-owned issuer (reviewer fixture           *)
(* a_own_registered_key: its own tuple evidenced by both channels)      *)
(* falsifies (i) while (ii) and (iii) hold; an honestly registered      *)
(* issuer IS an issuer. The per-honest-key sentence (ii) is the claim   *)
(* (the S-P3/S-P1 pattern); (i) is what this fixture adds, no more.     *)
(* This model does not prove: that no second, contradictory TLR under   *)
(* the same key exists (G4; probe F3); anything temporal about the      *)
(* TLR's anchor (ledger C-2, cross-formalism); that malformed standing  *)
(* evidence is rejected (carried check, inert; S-series vector); that   *)
(* S2/S3/S4 reason codes are distinct in the implementation (vector;    *)
(* ReasonCollapsed here checks the model's own constants only); P7 type *)
(* soundness of the TLR object or which identity a wrapped bundle's     *)
(* standing binds to (S-P7); the composition of the signed core or its  *)
(* encoding (P8); TLR/refusal-record consistency (SC-2, A3.9); anything *)
(* about lineages of other than two attempts; anything about the        *)
(* envelope (P4) verdict, which is carried in parallel and not queried. *)
(* CORRECTION 2026-09-06 (cross-family review items 1-3): also not      *)
(* proved, because UNREPRESENTABLE here — signature-set operations      *)
(* (A1.3 item 2: stripping, duplication, reordering; one signature slot *)
(* per object; S-P2's subject); byte-encoding attacks (non-canonical    *)
(* encodings, hash-input ambiguity, cross-kind parse confusion: term    *)
(* equality stands in for byte equality — a coverage failure, not an    *)
(* unreachable attack; P8 injectivity, library h/fp idealization); and  *)
(* anything about verdict values, waivers or policy inputs (P4).        *)
(*                                                                      *)
(* LOAD-BEARING checks — CORRECTION 2026-09-06 (cross-family review     *)
(* items 4 and 25; reviewer single-removal matrix, falsification-       *)
(* 2026-09-06/scratch/dependency-index.txt, line numbers cited): the    *)
(* pre-review list read "aid := h(core) computed by the verifier and    *)
(* looked up in the lineage (Q2); terminal = TERM_SHIPPED(aid) (Q4)";   *)
(* the lookup and the terminal check are load-bearing only JOINTLY.     *)
(* Load-bearing SINGLY (one removal turns a registered query red):      *)
(* honest-channel (repository) evidence over h(t) (matrix 3; abl5);     *)
(* fp(kT) = kfpr INSIDE the standing path (matrix 11; Q3); TLR          *)
(* signature under kT (matrix 13; abl6); aid := h(core) DERIVED by the  *)
(* verifier (matrix 18; Q2 replaces it by a presented label in lookup   *)
(* and terminal comparison at once).                                    *)
(* Load-bearing JOINTLY with each other, each single removal green on   *)
(* (i)-(iii): lookup2(aid, lin) (matrix 16, 17; MISMATCH witness lost); *)
(* terminal = TERM_SHIPPED(aid) (matrix 19, 20; S2/S4 witnesses lost);  *)
(* d = DISP_SHIPPED (matrix 21; abl3). Q4's red removes the terminal    *)
(* predicate AND the per-entry check together. The terminal check is    *)
(* load-bearing singly for the S2/S4 VOCABULARY, which SC-3 registers.  *)
(* CARRIED checks (inert on every registered query, single removal):    *)
(* DNS-channel evidence (matrix 2; abl4; the key is public); authority  *)
(* tag equalities (matrix 8-10); kT = kX (matrix 12; abl1) — a FIXTURE- *)
(* SHAPE restriction (probe tlr.pub = core.pub) that no registered text *)
(* requires (item 28); the TLR domain tag (matrix 14) — inert in this   *)
(* fixture, load-bearing under composition with a foreign-tag signer    *)
(* (item 27; library D-6 note); the anchor-proof binding (matrix 15;    *)
(* abl2; C-2); the whole envelope path (matrix 24-35; P4's verdict;     *)
(* S-P3's checks verbatim).                                             *)
```

---

## Author's answers

*(none yet — entered here, dated, in the author's words)*
