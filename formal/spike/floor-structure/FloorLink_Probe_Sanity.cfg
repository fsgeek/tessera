\* Witness cfg — run with TLC -continue; TLC VIOLATING all four is the
\* healthy result (each witness configuration is reachable).
CONSTANTS
  Stmts = {s1, s2}
  Keys = {k1, k2}

INIT Init
NEXT Next

INVARIANTS
  LinkedUnsatisfiable
  CardNeverAcceptsForeignOnly
  RelevantNeverAcceptsOpenChain
  RelevantNeverAcceptsForeignPossession
