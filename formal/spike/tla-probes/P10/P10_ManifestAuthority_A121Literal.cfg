\* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator; not adopted; the commit is the author's.
\* PROBE — NON-DISCHARGING (PROBE.md post-run note 2; RESULTS-PROBE.md O1). Reading
\* variant: A1.2.1's own words ("fewer than all") WITHOUT A3 §A3.2 item 1's tightening
\* (never zero) — Init admits waived = Channels. Each invariant is run in its OWN TLC
\* run by run-probe.sh (RECUT 3). Predicted red, registered before the run:
\* {NeverZero, UnavailabilityIsNotWaiver, PossessionNeverAuthority}; predicted green:
\* the other seven, StrictNeedsAuthorizedKey and DegradedNamesWaived in particular.
INIT Init
NEXT Next

INVARIANTS
  TypeOK
  Partition
  StrictNeedsAuthorizedKey
  StrictMeansBothAgree
  DegradedNamesWaived
  NeverSilent
  NeverZero
  UnavailabilityIsNotWaiver
  PossessionNeverAuthority
  LaterCaptureStrictUnreachable
