\* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator; not adopted; the commit is the author's.
\* PROBE — NON-DISCHARGING (PROBE.md Q6). Companion: each invariant is run in
\* its OWN TLC run by run-probe.sh (RECUT 3) so the whole red set is certified;
\* an invariant reported "No error" held on every state.
INIT Init
NEXT Next

INVARIANTS
  TypeOK
  Partition
  StrictNeedsAuthorizedKey
  StrictMeansBothAgree
  DegradedNamesWaived
  NeverSilent
  NeverZero
  UnavailabilityIsNotWaiver
  PossessionNeverAuthority
  LaterCaptureStrictUnreachable
