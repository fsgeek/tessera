\* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator; not adopted; the commit is the author's.
\* PROBE — NON-DISCHARGING (PROBE.md post-run note 3 item 3). Vacuity witnesses on the
\* _A32Reading variant, run ONE PER TLC RUN via run-probe.sh (RECUT 3). Added because the
\* variant's green (DegradedWaivedFailUnreachable holds) was certified with no witness that
\* the variant still reaches VALID_DEGRADED at all. Predicted (registered before run 5):
\*   VIOLATED (healthy): VerdictNever{Strict,Degraded,Invalid,Unverifiable},
\*     DegradedNonAuthorizedUnreachable, StrictNonAuthorizedUnreachable, LateEvidenceUnreachable,
\*     WhyNever_absent, WhyNever_unperformable, WhyNever_would_pass,
\*     UnverifiableViaAbsentUnreachable, InvalidViaConflictUnreachable.
\*   HOLD (the literal reading removes exactly these "why" values from records):
\*     WhyNever_invalid, WhyNever_anchor_late, WhyNever_names_other_key,
\*     DegradedWaivedFailUnreachable, UnverifiableWaivedFailUnreachable.
INIT Init
NEXT Next

INVARIANTS
  VerdictNeverStrict
  VerdictNeverDegraded
  VerdictNeverInvalid
  VerdictNeverUnverifiable
  DegradedNonAuthorizedUnreachable
  StrictNonAuthorizedUnreachable
  LateEvidenceUnreachable
  WhyNever_absent
  WhyNever_unperformable
  WhyNever_invalid
  WhyNever_anchor_late
  WhyNever_names_other_key
  WhyNever_would_pass
  UnverifiableViaAbsentUnreachable
  InvalidViaConflictUnreachable
  DegradedWaivedFailUnreachable
  UnverifiableWaivedFailUnreachable
