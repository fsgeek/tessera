\* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator; not adopted; the commit is the author's.
\* PROBE — NON-DISCHARGING (PROBE.md). Vacuity witnesses: run ONE PER TLC RUN
\* via run-probe.sh (RECUT 3: TLC reports only the first violated invariant per
\* state, so co-violated witnesses vanish under -continue). TLC VIOLATING every listed
\* formula is the healthy result — each violation exhibits a reachable state.
\* A formula NOT reported violated is a witness that did not fire (PROBE.md Q7).
\* ADDED 2026-09-06 (later still): UnverifiableWaivedFailUnreachable — the second divergence state (PROBE.md post-run note 3 item 2); predicted VIOLATED here.
INIT Init
NEXT Next

INVARIANTS
  VerdictNeverStrict
  VerdictNeverDegraded
  VerdictNeverInvalid
  VerdictNeverUnverifiable
  DegradedNonAuthorizedUnreachable
  StrictNonAuthorizedUnreachable
  LateEvidenceUnreachable
  WhyNever_absent
  WhyNever_unperformable
  WhyNever_invalid
  WhyNever_anchor_late
  WhyNever_names_other_key
  WhyNever_would_pass
  UnverifiableViaAbsentUnreachable
  InvalidViaConflictUnreachable
  DegradedWaivedFailUnreachable
  UnverifiableWaivedFailUnreachable
