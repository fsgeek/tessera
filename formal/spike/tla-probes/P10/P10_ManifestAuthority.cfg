\* STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator; not adopted; the commit is the author's.
\* PROBE — NON-DISCHARGING (PROBE.md). Correct model, main invariants.
\* Run: java -DTLA-Library=<repo>/formal/tla -XX:+UseParallelGC -jar tla2tools.jar -config P10_ManifestAuthority.cfg -workers 4 P10_ManifestAuthority.tla
INIT Init
NEXT Next

INVARIANTS
  TypeOK
  Partition
  StrictNeedsAuthorizedKey
  StrictMeansBothAgree
  DegradedNamesWaived
  NeverSilent
  NeverZero
  UnavailabilityIsNotWaiver
  PossessionNeverAuthority
  LaterCaptureStrictUnreachable
