CONSTANTS
  Checks = {c1, c2}
  NonWaivable = {c1}
  Machines = {mA, mB}
  ServiceStates = {"unreachable"}
  MaxRuns = 2
  RepeatOnMachine = FALSE
  Verifier = "PURE"

INIT Init
NEXT Next

\* Run 3 (2026-09-06, after skeptic review): NeverRepeatMachine ALONE, so
\* that PROBE.md Q2's "must hold in VECTOR" is a clean termination rather
\* than silence inside a multi-witness list (F-T1). Green is the healthy
\* result here: two fresh machines never repeat.
INVARIANTS
  NeverRepeatMachine
