CONSTANTS
  Checks = {c1, c2}
  NonWaivable = {c1}
  Machines = {mA, mB}
  ServiceStates = {"authorized", "revoked", "unreachable"}
  MaxRuns = 2
  RepeatOnMachine = TRUE
  Verifier = "LIVE_FALLBACK"

INIT Init
NEXT Next

\* Vacuity witnesses, list 1 — run with -continue; a VIOLATION is the
\* healthy result for each witness PROBE.md Q2/Q6 says must fire. ORDER
\* MATTERS: TLC reports one violated invariant per state, so each entry
\* is placed where some state violates it and none listed above it.
INVARIANTS
  NeverValidStrict
  NeverRepeatMachine
  NeverTwoMachines
  NeverDecidedDead
  NeverConsulted
