CONSTANTS
  Checks = {c1, c2}
  NonWaivable = {c1}
  Machines = {mA, mB}
  ServiceStates = {"authorized", "revoked", "unreachable"}
  MaxRuns = 2
  RepeatOnMachine = TRUE
  Verifier = "LIVE_FALLBACK"

INIT Init
NEXT Next

\* Run 3 (2026-09-06, after skeptic review): NeverDecidedUnconsulted ALONE,
\* so that PROBE.md Q6's "holds" is a clean termination rather than
\* silence inside _Sanity2 (F-T1). Green is the healthy result: this
\* companion never decides without consulting.
INVARIANTS
  NeverDecidedUnconsulted
