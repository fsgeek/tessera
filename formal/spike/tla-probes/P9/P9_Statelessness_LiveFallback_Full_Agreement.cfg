CONSTANTS
  Checks = {c1, c2}
  NonWaivable = {c1}
  Machines = {mA, mB}
  ServiceStates = {"authorized", "revoked", "unreachable"}
  MaxRuns = 2
  RepeatOnMachine = TRUE
  Verifier = "LIVE_FALLBACK"

INIT Init
NEXT Next

\* Companion run: Agreement alone (the A4.1 vector, first clause).
INVARIANTS
  Agreement
