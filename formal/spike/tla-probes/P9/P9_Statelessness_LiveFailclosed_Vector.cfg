CONSTANTS
  Checks = {c1, c2}
  NonWaivable = {c1}
  Machines = {mA, mB}
  ServiceStates = {"unreachable"}
  MaxRuns = 2
  RepeatOnMachine = FALSE
  Verifier = "LIVE_FAILCLOSED"

INIT Init
NEXT Next

\* Companion run: Purity alone (Agreement has its own cfg — TLC reports
\* one violated invariant per state, see run1-shadowed/README.md).
INVARIANTS
  TypeOK
  Purity
