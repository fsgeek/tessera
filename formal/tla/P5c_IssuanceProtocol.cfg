\* MaxTime = 14 (raised from 8, 2026-07-20; 12 -> 14 same day on Codex
\* finding 5): the refusal invariants need the FINAL attempt's window to
\* be expirable within the bound — minimum MaxAttempts * (Delta + 1) = 12,
\* below which refusal is unreachable and NoSilentDeadlock passes
\* VACUOUSLY (the _Sanity RefusalUnreachable witness catches this). The
\* + DepthK headroom (14) additionally exercises the post-refusal burial
\* path — at 12 refusal lands exactly at the bound and no Tick can deepen
\* an anchor afterwards (the RefusalBuriedAnchorUnreachable witness
\* catches THAT).
\* CORRECTION 2026-09-06 (review item 15; entered by the AI collaborator,
\* PROPOSED, not adopted; the commit is the author's): the paragraph above was not
\* true of the witness as it stood — it fired at 12. It is true of the
\* recut witness (anchorAt >= refusedAt): unreachable at 12 and 13,
\* reachable at 14. RefusalTimeConsistent added (guard on refusedAt).
CONSTANTS
  MaxTime = 14
  Delta = 3
  DepthK = 2
  MaxAttempts = 3

INIT Init
NEXT Next

INVARIANTS
  NoShippedOrphan
  ShippedIsSound
  ExpiredCannotShip
  NoSilentDeadlock
  RefusedOnlyWhenExhausted
  RefusalTimeConsistent

\* "Durably recorded": the refusal record latches (action property).
PROPERTIES
  RefusalLatched
