\* Vacuity check: run with `tlc2.TLC -continue`. TLC VIOLATING all four
\* invariants is the healthy result — each violation exhibits a reachable
\* state satisfying a defined-predicate antecedent, proving the main
\* cfg's implication-shaped invariants are not vacuously true.
\* Order matters (most specific first): NonMonotonicAccept is a subset of
\* Acceptance; LateBurial witnesses the A2.0 artifact for
\* AbandonedArtifactRejected.
CONSTANTS
  MaxTime = 6
  DeltaMax = 3
  EpsilonMax = 1
  RcptTolMax = 6

INIT Init
NEXT Next

INVARIANTS
  NonMonotonicAcceptUnreachable
  AcceptanceUnreachable
  LateBurialCaseUnreachable
  HonestCostCaseUnreachable
