# TLA-fixes log — collaborator-owed items from the 2026-09-06 cross-family review

**STATUS: PROPOSED — 2026-09-06 — produced by the AI collaborator; not
adopted; the commit is the author's.**

Source of the items: `docs/reviews/2026-09-06-codex-tla-falsification-p4-p5p6-p5c.md`
("Consolidated findings", items 4, 5, 6, 8, 9, 12, 15, 16, 17, 18, 19) and
`docs/phase-0-prereg-amendment-4.md` §A4.2, §A4.3. Scope: `formal/tla/` only.
Discipline: amend-don't-rewrite (no committed header text deleted; every
change is a dated `CORRECTION 2026-09-06:` / `RECUT 2026-09-06:` addition);
no git state changed by the collaborator.

**Tooling and timebox.** TLC2 Version 2026.07.31.184830 (rev: 30cc360), as
printed on line 1 of every regenerated `.out` (`~/.local/lib/tla2tools.jar`,
manifest `Implementation-Version: 2.0 2026-07-31`, built 2026-07-31; the twelve
untouched `P5cP5P6_Bridge*`/`BridgeSlack*` `.out` files still carry the earlier
"TLC2 Version 2.19 of 08 August 2024" they were produced with; "TLC2 2.19"
stood here until corrected 2026-09-06, skeptic finding), OpenJDK 21.0.12, `java -XX:+UseParallelGC -jar … -config <cfg> -workers 4 <module>.tla`,
`-deadlock` on the P5c family (its header says so: terminal states deadlock
at the MaxTime bound), `-continue` on every `_Sanity` cfg. No TLA+ timebox
is registered anywhere in the record (the registered timeboxes are S-P3's
ProVerif ones, 15–30 min); the collaborator applied `timeout 1800` (30 min,
the largest registered value) to every run. No run timed out; the longest
was 2 min 34 s (P5P6 main). Every `.out` in `formal/tla/` whose module or
cfg changed was regenerated 2026-09-06 so that trace line numbers match the
committed modules (item 19). Modules NOT touched and NOT re-run:
`P5cP5P6_Bridge*.tla`, `P5cP5P6_BridgeSlack*.tla` (their `.out` files are
unchanged and still match).

**Mechanism note (not fixed; outside `formal/tla/`).**
`scripts/filter-tlc-output.sh` keys on the line form `Error: Invariant X is
violated.` and does not match TLC's stateless-model form `Error: Invariant X
is violated by the initial state:`. It therefore filters the P5c sanity dump
(as designed) but passes the P4 and P5P6 sanity dumps through unfiltered.
`P4_VerifierStates_Sanity.out` is committed unfiltered (14,560 lines, as the
previous record was); `P5P6_TemporalRevocation_Sanity.out` was elided with a
one-off initial-state-aware variant of the same awk (first violation per
witness kept, per-invariant counts from the raw stream printed in the
elision note, raw 11.86M lines not archived) — the same shape as the
previously committed evidence file. Extending the script is a one-line
change for the author to make or decline.

## Item-by-item

| # | Item | Change | Run | Result |
|---|---|---|---|---|
| 15 | P5c `RefusalBuriedAnchorUnreachable` encodes no chronology (CONFIRMED) | **Recut.** `P5c_IssuanceProtocol.tla`: new variable `refusedAt` (sentinel `NoRefusal = -1`; the refusing Tick stamps `now + 1`, i.e. the clock value it moves to; every other action leaves it unchanged); new guard invariant `RefusalTimeConsistent == (refused <=> refusedAt # NoRefusal) /\ (refused => refusedAt \in 0..now)` added to the main cfg; witness becomes `~(refused /\ anchorAt >= refusedAt /\ depth >= DepthK)`. An anchor placed before the refusing Tick has `anchorAt <= refusedAt - 1`, so the new conjunct is exactly "landed at or after the refusal instant". Every pre-existing invariant and the action property kept unchanged. `_Broken` and `_BrokenSilent` carry the variable; `_BrokenSilent` additionally carries `RefusalTimeConsistent` (in it, `Refuse` stamps `refusedAt' = now`); `_Broken` keeps its single invariant `NoShippedOrphan` by design (its cfg is untouched). Their "identical state space and actions" headers stay literally true; `_BrokenSilent.cfg` and `_BrokenSilent_Green.cfg` check the new invariant. Dated notes in the module header, at `Anchor`, at the witness, and in both cfgs; PROPERTIES.md P5c bullet gains an appended dated note. | main; `_Sanity` (-continue, filtered); `_Broken`; `_BrokenSilent`; `_BrokenSilent_Green`; plus recut checks `_Sanity` at MaxTime 12 and 13 (`P5c/recut/Sanity_MaxTime{12,13}.{cfg,out}`) | main **green** (6 invariants + `RefusalLatched`; 3606 generated / 2190 distinct — identical to before: `refusedAt` is determined by the rest of the state). `_Sanity` at 14: **all seven witnesses fire**; the buried-anchor trace is refusal at now = 12 (`refusedAt = 12`), `Anchor` at 12, depth 2 at now = 14. Recut checks: at **12 and 13 the buried-anchor witness does NOT fire** (other six do), at 14 it does — the "+DepthK headroom = 14" sentence is now true of the witness. `_Broken` **red on `NoShippedOrphan`** (anchor → tick → ship → reorg, as before). `_BrokenSilent` **red on `NoSilentDeadlock`** (straight-line exhaustion, as before). `_BrokenSilent_Green` **green** with `RefusalTimeConsistent` in the set (3874/2400 states; grew from 3825/2310 because `refusedAt` varies with when `Refuse` fires). |
| 19 | Stale line numbers in committed traces | Regeneration (all `.out` in `formal/tla/` for the P4, P5P6, P5c families) | see above | Checked by hand: `Tick line 200`, `Anchor line 226`, `Ship line 261` in `P5c_IssuanceProtocol_Sanity.out` match the recut module; `_Broken` (`Ship line 76`) and `_BrokenSilent` (`Tick line 61`) likewise. |
| 4 | `_Green` isolation configs for P4 and P5P6 companions | `P4_VerifierStates_Broken_Green.cfg` (the seven minus `StrictMeansEverything`); `P5P6_TemporalRevocation_BrokenConf_Green.cfg` (`ForgeryRejected`, `ReceiptIndependence`); `P5P6_TemporalRevocation_BrokenTolStrict_Green.cfg` (`ForgeryRejected`, `WindowRespected`, `VerifierOwnsTolerances`). **Not applicable, recorded:** `P5P6_TemporalRevocation_Broken.cfg` and `_BrokenTol.cfg` each configure ONE invariant (their modules define only that one), so "every configured invariant except the red one" is the empty set; item 4's own disposition was "no model change", so no invariants were added to those modules. (Same for `P5c_IssuanceProtocol_Broken.cfg`, single invariant, not in this item's scope.) | the three `_Green` cfgs | **P5P6 `_BrokenConf_Green`: green** (4,302,592 initial states, 2 min 17 s). **P5P6 `_BrokenTolStrict_Green`: green** (24 s). **P4 `_Broken_Green`: RED on `NoSilentPromotion`** — predicted in the cfg before running, and it is a finding, recorded below (F1), not hidden. |
| F1 | (finding from item 4) P4 fail-open break is not one-invariant | Recorded in the `_Green.cfg` comment, the `_Broken` module's corrected comment, and here. Second-stage `P4_VerifierStates_Broken_Green2.cfg` checks the residue with the whole implicated set removed (`StrictMeansEverything`, `NoSilentPromotion`, `ValidNeedsNonWaivablePass`). `ValidNeedsNonWaivablePass` is masked in `_Green` (every state that fails it also fails `NoSilentPromotion`, listed first; TLC reports the first per state), so it was confirmed red on its own by an archived one-invariant run, `P4/Broken_VNWP_alone.{cfg,out}`. | `_Green` (red), `_Green2`, `Broken_VNWP_alone` | `_Green` red on `NoSilentPromotion` at `c2 = unperformable, waived = {}` (broken `VerdictFor` returns `VALID_STRICT`). `Broken_VNWP_alone`: red on `ValidNeedsNonWaivablePass`, same state. **`_Green2`: green** on `Partition`, `Monotonicity`, `DegradedNeedsExplicitWaiver`, `UnverifiableIsHonest` (the last vacuous in this module — the broken function never returns `UNVERIFIABLE`; the review already says so). Net: the fail-open defect turns red exactly the three formulas that speak about unperformable checks; the three that do not stay green; one is vacuous. The `_Broken` header's "NoSilentPromotion, or StrictMeansEverything" is therefore incomplete but not wrong; not edited beyond the dated comment at "Same invariants". |
| 5 | `P4_Broken` "same invariants" comment incomplete | Dated `CORRECTION` under the comment: the first seven of eleven; the four `Exact*` formulas not carried; `UnverifiableIsHonest` vacuous here; pointer to the `_Green` results. | `_Broken` re-run | red on `StrictMeansEverything` at `c5 = unperformable, waived = {}` — same counterexample as before. |
| 6 | P4 header "machine-checkable form of P4" overstates | Dated `CORRECTION` block in the header: what the eleven formulas discharge (over `[Checks -> Statuses] × SUBSET Waivable` for the configured instance); what is carried elsewhere (recording/authorization → H1a; `c1..c5` category mapping → reading aid; bundles/keys/replay → P1/P3/P7 and the symbolic suite); verdict is derived not stored. Plus the two A4.2 citations below. | P4 main; `_Sanity` (-continue) | main **green** (11 invariants, 1944 states). `_Sanity`: all four `VerdictNever*` witnesses fire (counts 1 / 63 / 1444 / 436 — first-violation-per-state, as before). |
| A4.2 | precedence ratified; "required" qualifier | Header block cites Amendment 4 §A4.2 for the FAIL-over-UNPERFORMABLE precedence ("Precedence — RULED (author)", quoted with its file) and for reading P4's sentence as "a **required** check that cannot be performed…"; a one-line dated note at the `VerdictFor` comment (lines 49–52, "for author ratification") says the same. The citation carries the caveat that `docs/phase-0-prereg-amendment-4.md` (committed `5188e7a` and OTS-stamped at `5dfd82b`) still reads "adopted in session, not yet signed" in its own status line; its own in-force criterion is "the author's signing commit and OpenTimestamps stamp", so whether the stamp or the status line governs is the author's call. | (same runs) | — |
| 12 | P5P6 header "exact by construction" | Dated `CORRECTION` in the header: the join was asserted in comments and checked nowhere until `P5cP5P6_Bridge.tla` checked it as `ShippedDesignatedAgree` (`_BrokenAnchorSubst` red); under the fused clock the guards merely coincide. | P5P6 main; `_Sanity` | main **green** (7 invariants, 4,302,592 states, 2 min 34 s). `_Sanity`: all four witnesses fire with counts identical to the previous record (441,098 / 101,528 / 267,736 / 267,932). |
| 8 | `ReceiptIndependence` "strictly stronger" | Dated `CORRECTION` at the invariant: stronger for receipt INFLUENCE only; not logically stronger than `VerifierOwnsTolerances`; complementary. Header block also records that `VerifierOwnsTolerances` compares against the global maxima, not the chosen policy, and that ownership is carried by `ReceiptIndependence`. | (same) | — |
| A4.3 | revocation after the anchor | Dated note at `AuthorizedAtDeclared` citing §A4.3 ("after both `anchor_time` and `declared_issue_time`") and stating the item-13 boundary (`declared < revoked` stands in for `key_authorized(declared)`, no activation time). | (same) | — |
| 9 | `_BrokenTol` header witness quantity | Dated `CORRECTION` in the header: the printed counterexample has anchor-to-declared gap 0; the red conjunct is `confirmedAt - declared <= DeltaMax` (4 > 3) via `rcptDelta = 4`. | `_BrokenTol` re-run | red on `VerifierOwnsTolerances`, same counterexample as before (`anchor = declared = 0`, `confirmedAt = 4`, `rcptDelta = 4`). Also re-run: `_Broken` (red `ForgeryRejected`), `_BrokenConf` (red `AbandonedArtifactRejected`), `_BrokenTolStrict` (red `ReceiptIndependence`) — all as before. |
| 17 | P5c "IS the chain-time predicate" | Dated `CORRECTION` in the header (item 17 bullet): coincidence holds only under the single-clock abstraction; `ShippedIsSound` has no confirmation-time conjunct and no ε; A2.1 calls the fused guard "locally stronger"; the decoupled predicate and its agreement with `Ship` are the bridge's (`ShippedDesignatedAgree`). | (item 15 runs) | — |
| 16 | P5c latch pointer | Dated `CORRECTION` in the header (item 16 bullet): eligibility latching is the registered lifecycle and is modelled and checked in `P5cP5P6_BridgeSlack_Latch.tla`; this module is the S = 0, pre-latch instance in which a timely-eligible final attempt can be expired by a Tick before Ship (a property of the instance, not of the lifecycle). | (item 15 runs) | — |
| 18 | `P5c_Broken` "only inclusion (depth >= 1)" off by one | Dated `CORRECTION` in the header and at the broken `Ship`: depth >= 1 is one block after inclusion (inclusion is depth 0); shallow, not "mere inclusion"; the expected trace already shows one confirmation. | `_Broken` re-run | red on `NoShippedOrphan`, trace anchor → tick → ship → reorg. |

## Files touched (all under `formal/tla/`)

Modified modules: `P4_VerifierStates.tla`, `P4_VerifierStates_Broken.tla`,
`P5P6_TemporalRevocation.tla`, `P5P6_TemporalRevocation_BrokenTol.tla`
(comment-only, four files); `P5c_IssuanceProtocol.tla`,
`P5c_IssuanceProtocol_Broken.tla`, `P5c_IssuanceProtocol_BrokenSilent.tla`
(recut: one variable, one invariant, one witness).
Modified cfgs: `P5c_IssuanceProtocol.cfg`, `P5c_IssuanceProtocol_Sanity.cfg`,
`P5c_IssuanceProtocol_BrokenSilent.cfg`, `P5c_IssuanceProtocol_BrokenSilent_Green.cfg`.
New cfg/out pairs: `P4_VerifierStates_Broken_Green`, `P4_VerifierStates_Broken_Green2`,
`P5P6_TemporalRevocation_BrokenConf_Green`, `P5P6_TemporalRevocation_BrokenTolStrict_Green`.
Regenerated `.out`: every P4, P5P6, and P5c file.
Archived checks: `falsification-2026-09-06/P5c/recut/Sanity_MaxTime{12,13}.{cfg,out}`,
`falsification-2026-09-06/P4/Broken_VNWP_alone.{cfg,out}`; (second pass)
`falsification-2026-09-06/filter-tlc-initial-state.awk`.
Outside `formal/tla/`: one appended dated note to `formal/PROPERTIES.md`'s
P5c (A2.3 refusal) bullet; nothing else.
TLC's `*_TTrace_*.bin/.tla` by-products were deleted (never part of the record).

## What this log does not do

It moves no tracker row, adopts nothing, and edits no registered text. The
review's routed items 1, 2, 10 are the author's (Amendment 4 §A4.2/§A4.3
carry them; its status line governs). Reading aids for these three modules
(ENUMERATION note 5 item 2) remain owed separately.

## Verification pass — 2026-09-06, later the same day (AI collaborator; PROPOSED)

A second pass over the working tree before handing the summary to the
author, checking the log above against the files rather than against
memory. Nothing above was altered; this section is appended.

- **Every `.out` postdates the module it was produced from.** [Amended later
  the same day, second pass: after the re-runs recorded below, every
  regenerated `.out` also postdates its `.cfg`; the mtimes quoted here are
  those of the first pass and are superseded by the table in the second
  pass.] File mtimes:
  `P4_VerifierStates.tla`, `P4_VerifierStates_Broken.tla`,
  `P5P6_TemporalRevocation.tla`, `P5P6_TemporalRevocation_BrokenTol.tla`
  at 18:32:36; their `.out` files 18:33:03 – 18:39:45.
  `P5c_IssuanceProtocol_Broken.tla`, `_BrokenSilent.tla` at 18:30:57; their
  `.out` files 18:31:27 – 18:31:28. `P5c_IssuanceProtocol.tla` at 18:39:48
  (header comments finished after the companions ran; the companions are
  self-contained modules and do not import it); its main and `_Sanity`
  `.out` and the two archived recut checks at 18:42:04 – 18:42:07.
- **Trace line numbers resolve to the named actions** (item 19): every
  `line N … of module M` reference in the P5c `.out` files was looked up in
  the committed module — `P5c_IssuanceProtocol` 200 Tick / 226 Anchor / 237
  Reorg / 261 Ship / 271 Reissue; `_Broken` 51 / 60 / 66 / 76; `_BrokenSilent`
  61 Tick / 89 Reissue (`Refuse`, line 98, appears in no committed trace: the
  `NoSilentDeadlock` counterexample is the run in which it never fires; this
  entry read "89 Refuse" until corrected 2026-09-06, skeptic finding). The P4 and P5P6 families are stateless (violations are
  reported "by the initial state") and carry no line references.
- **Verdicts as logged.** Green: P4 main (1944 states), P4 `_Broken_Green2`,
  P5P6 main and both P5P6 `_Green` cfgs (4,302,592 states each), P5c main
  (3606/2190) and `_BrokenSilent_Green` (3874/2400). Red on the expected
  invariant only: P4 `_Broken` (StrictMeansEverything), P5P6 `_Broken`
  (ForgeryRejected), `_BrokenConf` (AbandonedArtifactRejected), `_BrokenTol`
  (VerifierOwnsTolerances), `_BrokenTolStrict` (ReceiptIndependence), P5c
  `_Broken` (NoShippedOrphan), `_BrokenSilent` (NoSilentDeadlock). Red as
  predicted and recorded as finding F1: P4 `_Broken_Green` (NoSilentPromotion)
  and the archived `Broken_VNWP_alone` (ValidNeedsNonWaivablePass). Sanity:
  P4 four witnesses (1 / 63 / 1444 / 436), P5P6 four witnesses (elided,
  counts in the file's note), P5c seven witnesses at 14 and six at 12 and 13
  (`RefusalBuriedAnchorUnreachable` silent at 12 and 13, as the recut
  requires; the 14 trace has `refusedAt = 12`, anchor at 12, depth 2 at 14).
- **Single-invariant companions confirmed by reading:**
  `P5P6_TemporalRevocation_Broken.tla` defines only `ForgeryRejected`,
  `_BrokenTol.tla` only `VerifierOwnsTolerances`, `P5c_IssuanceProtocol_Broken.tla`
  only `NoShippedOrphan` — so the item-4 `_Green` set for each is empty, as
  recorded above.
- **One edit in this pass, comment-only:** the four P5c cfg recut notes
  (`P5c_IssuanceProtocol.cfg`, `_Sanity.cfg`, `_BrokenSilent.cfg`,
  `_BrokenSilent_Green.cfg`) gained the provenance label ("entered by the AI
  collaborator, PROPOSED, not adopted; the commit is the author's") that the
  module headers and the new `_Green` cfgs already carried. TLC does not read
  cfg comments, so the committed `.out` files are unaffected and were not
  re-run. [Superseded later the same day: the four `.out` files WERE re-run
  after this edit so that every input, cfg included, predates its output —
  see the second verification pass below.]
- **Record inconsistency flagged for the author, not fixed here:** the
  header corrections and this log cite `docs/phase-0-prereg-amendment-4.md`
  with the caveat that its own status line reads "DRAFT — adopted in
  session, not yet signed" (still true of the committed file at `5188e7a`,
  which is the author's commit and is OTS-stamped at `5dfd82b` — the two
  things the document names as its in-force criterion; the status line was
  simply not updated),
  while the working-tree `formal/PROPERTIES.md` (edited outside this
  artifact) calls Amendment 4 "signed 2026-09-06, `5188e7a`". One of the two
  should change; which one is the author's call.
- No TLC `*_TTrace_*` by-products remain in `formal/tla/`; `formal/tla/states/`
  holds checkpoint directories from the error-exiting runs and is gitignored.

## Second verification pass — 2026-09-06, later still (AI collaborator; PROPOSED)

A skeptic reviewed this log and the working tree and returned eight
findings (two defects, five nits, one informational). Disposition, with the
principle that a log asserting "every reference was looked up" must itself
survive being looked up:

- **Defect, applied in place (log line "`_BrokenSilent` 61 Tick / 89 Refuse").**
  Wrong. `Reissue ==` is at line 88 (body 89–93) and `Refuse ==` at line 98 of
  `P5c_IssuanceProtocol_BrokenSilent.tla`; the committed trace contains
  `<Reissue line 89` (2) and `<Tick line 61` (12) and no `Refuse` step — the
  `NoSilentDeadlock` counterexample is the run in which `Refuse` never fires.
  Corrected to "89 Reissue" with the original wording noted. The other
  line-number lookups in that bullet were re-resolved and stand.
- **Defect, applied in place ("TLC2 2.19").** No regenerated artifact says
  2.19: every regenerated `.out` begins `TLC2 Version 2026.07.31.184830 (rev:
  30cc360)`; the jar manifest says `Implementation-Version: 2.0 2026-07-31`,
  `X-Git-BuildNumber: master_2026.07.18.145032;v1.8.0_30cc360`. The twelve
  untouched Bridge/BridgeSlack `.out` files do begin `TLC2 Version 2.19 of 08
  August 2024 (rev: 5a47802)` — the earlier tool, which is where the wrong
  string came from. Tooling paragraph corrected; two TLC builds are now
  named, each against the files it produced.
- **Nit, applied (item 15 row).** "`_Broken` and `_BrokenSilent` carry the
  variable (…) and `RefusalTimeConsistent`" read as if both companions carry
  the invariant. Only `_BrokenSilent.tla` defines it (lines 134–136);
  `_Broken.tla`'s sole invariant is `NoShippedOrphan` (line 93) and
  `_Broken.cfg` is untouched. Reworded.
- **Nit, applied by regeneration (cfg mtimes).** The four P5c cfgs (19:15:35)
  postdated their `.out` files (18:31–18:42), and the cfg diff against HEAD
  carries a non-comment line (`RefusalTimeConsistent` under INVARIANTS), which
  TLC does not echo into the `.out`; the reader had only this log's word that
  the 19:15 edit was comment-only. Rather than ask the reader to take that
  word, the four `.out` files were regenerated (main, `_Sanity` through the
  committed `scripts/filter-tlc-output.sh`, `_BrokenSilent`,
  `_BrokenSilent_Green`; `-deadlock`, `-workers 4`, `timeout 1800`, longest
  run 1 s). Verdicts and state counts identical to the first pass: main
  **green** 3606/2190; `_BrokenSilent_Green` **green** 3874/2400;
  `_BrokenSilent` **red** on `NoSilentDeadlock`; `_Sanity` **all seven
  witnesses fire**, 506 repeats elided by the committed script. Every
  regenerated `.out` now postdates both its module and its cfg (table below).
- **Nit, applied (`P5P6_TemporalRevocation.tla`, CORRECTION at
  `ReceiptIndependence`).** "(e.g. an oversized polDelta)" named a value
  outside the state space — `Init` bounds `polDelta \in 0..DeltaMax`. The
  example is now a predicate ("hard-codes a bound larger than DeltaMax and
  ignores its receipt arguments"), with the Init bound cited. Comment-only.
- **Nit, applied (`P4_VerifierStates.tla` header caveat; log rows A4.2 and
  "Record inconsistency").** The caveat reported Amendment 4's status line
  ("adopted in session, not yet signed" at `5188e7a`) without the other two
  facts in the git record: `5188e7a` is the author's commit and `5dfd82b`
  ("ots: stamp 5188e7a…") is its OpenTimestamps stamp — the two things the
  document itself names as its in-force criterion. Both facts now appear
  beside the status-line fact in all three places; which governs is left as
  the author's call, in those words. Comment-only.
- **Nit, half applied (`scripts/filter-tlc-output.sh`).** The script edit is
  outside `formal/tla/` and is the author's to make or decline — **declined
  here**, unchanged. What could be done inside `formal/tla/` was: the
  initial-state-aware awk variant is now archived as
  `falsification-2026-09-06/filter-tlc-initial-state.awk` (PROPOSED), and
  `P5P6_TemporalRevocation_Sanity.out` was regenerated through it, so the
  elided evidence file reproduces from recorded tooling. Its elision note
  names the archived file and lists the counts in first-seen order rather
  than alphabetically; the four witness states and the four counts are
  byte-identical to the first pass (441,098 / 101,528 / 267,736 / 267,932;
  1,078,290 elided; raw stream 11,861,268 lines).
- **Informational finding:** no change requested; noted with thanks. One of
  its observations does not generalise, see next bullet.

**Consequential re-runs.** The two comment-only edits above touched
`P4_VerifierStates.tla` and `P5P6_TemporalRevocation.tla` and would have left
those modules newer than their `.out` files — the same ordering the cfg nit
objects to. Both families' companions are self-contained (`EXTENDS
FiniteSets` / `EXTENDS Integers` only; no companion imports the main module),
so only the main and `_Sanity` cfgs of each were re-run: P4 main **green**
1944 states (1 s); P4 `_Sanity` four witnesses 1 / 63 / 1444 / 436,
unfiltered as before (14,560 lines); P5P6 main **green** 4,302,592 states
(2 min 32 s); P5P6 `_Sanity` as above (14 s). P4 and P5P6 outputs are
identical to the first pass modulo seed/pid/timestamp lines.

**Mechanism observation — first-reported counterexamples are not
run-stable under `-workers 4`.** The skeptic's re-run found every
counterexample byte-identical to the committed one; this pass did not, and
five further `_BrokenSilent` runs show why. TLC stops at the first violation
any worker reaches, so both the trace it prints and the "states generated"
figure of an error-exiting run depend on worker scheduling: `_BrokenSilent`
gave 1564/1022 (first pass), 1625/1056 (this pass), and 1582/1032, 1607/1045,
1634/1063 (three scratch repeats); the printed trace was Tick-first in every
run but the regenerated one, which opens Anchor-at-0 then Ticks. In the
`_Sanity` run, five of the seven first-violation blocks are byte-identical to
the first pass (including `RefusalBuriedAnchorUnreachable`: `refusedAt = 12`,
`Anchor` at 12, depth 2 at `now = 14`); `RefusalWithLiveAnchorUnreachable` and
`RefusalAfterReorgUnreachable` report a different first state. Verdicts, the
set of red invariants, and complete-search state counts are stable and are
what the record claims; the "straight-line exhaustion" wording in the item 15
row describes the first-pass trace and is now one shape the counterexample
can take. Green runs and the P4/P5P6 stateless families (violations "by the
initial state", enumerated in a fixed order) showed no such variance. A record
claiming a specific trace shape should say "one such trace" or run with
`-workers 1`; no registered command line was changed here.

**Input/output ordering after this pass** (mtimes, 2026-09-06):

| `.out` | written | `.cfg` | `.tla` |
|---|---|---|---|
| `P4_VerifierStates` | 19:37:13 | Jul 23 | 19:35:17 |
| `P4_VerifierStates_Sanity` | 19:37:13 | Jul 23 | 19:35:17 |
| `P5P6_TemporalRevocation` | 19:39:47 | Jul 23 | 19:35:17 |
| `P5P6_TemporalRevocation_Sanity` | 19:38:29 | Jul 23 | 19:35:17 |
| `P5c_IssuanceProtocol` | 19:34:52 | 19:15:35 | 18:39:48 |
| `P5c_IssuanceProtocol_Sanity` | 19:34:53 | 19:15:35 | 18:39:48 |
| `P5c_IssuanceProtocol_BrokenSilent` | 19:34:53 | 19:15:35 | 18:30:57 |
| `P5c_IssuanceProtocol_BrokenSilent_Green` | 19:34:54 | 19:15:35 | 18:30:57 |

All other `.out` files are as the first pass left them and their inputs are
unchanged since. TLC's `*_TTrace_*` by-products from the error-exiting run
were deleted again. Nothing outside `formal/tla/` was touched in this pass;
no git state was changed.
