\* CHECK 2026-09-06: ValidNeedsNonWaivablePass alone on the _Broken module,
\* to confirm by run (not only by reading) that it is red and merely masked
\* in _Green by NoSilentPromotion. EXPECTED: RED.
CONSTANTS
  Checks = {c1, c2, c3, c4, c5}
  NonWaivable = {c1, c2}

INIT Init
NEXT Next

INVARIANTS
  ValidNeedsNonWaivablePass
