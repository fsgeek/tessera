\* The off-by-one control, on the MAIN bridge module: DepthK = KConf = 3
\* is the natural transcription mistake (forgetting that the including
\* block is already the first confirmation). EXPECTED: RED on
\* PinAgreement, reported in TLC's constant-invariant form — "The
\* invariant of PinAgreement is equal to FALSE", before any state is
\* explored (stronger than a trace: the off-by-one cannot survive
\* configuration). This demonstrates
\* the pin is checked, not assumed: an off-by-one in the cfg (or in
\* either side's transcription) cannot pass silently.
CONSTANTS
  MaxTime = 6
  Delta = 2
  Epsilon = 1
  KConf = 3
  DepthK = 3
  MaxSkew = 2
  MaxBlocks = 4

INIT Init
NEXT Next

INVARIANTS
  PinAgreement
  ShippedDesignatedAgree
