\* EXPECTED: RED on NoSilentDeadlock — the separately enabled Refuse can
\* be postponed past the final deadline crossing, so the silent-deadlock
\* state is reachable. The _Green companion cfg proves every OTHER
\* invariant still holds, i.e. the break is isolated to exactly the
\* deadlock invariant AMONG THE CHECKED SET (Codex finding 4 scoping).
CONSTANTS
  MaxTime = 14
  Delta = 3
  DepthK = 2
  MaxAttempts = 3

INIT Init
NEXT Next

INVARIANTS
  NoShippedOrphan
  ShippedIsSound
  ExpiredCannotShip
  RefusedOnlyWhenExhausted
  NoSilentDeadlock

PROPERTIES
  RefusalLatched
