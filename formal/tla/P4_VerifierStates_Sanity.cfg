\* Vacuity check: run with `tlc2.TLC -continue`. TLC VIOLATING all four
\* invariants is the healthy result — each violation exhibits a reachable
\* state with that verdict, proving the main cfg's implication-shaped
\* invariants are not vacuously true.
CONSTANTS
  Checks = {c1, c2, c3, c4, c5}
  NonWaivable = {c1, c2}

INIT Init
NEXT Next

INVARIANTS
  VerdictNeverStrict
  VerdictNeverDegraded
  VerdictNeverInvalid
  VerdictNeverUnverifiable
