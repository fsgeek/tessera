\* Second-stage isolation control (added 2026-09-06 by the AI
\* collaborator, PROPOSED; see FIXES-2026-09-06.md). The _Green run was
\* RED on NoSilentPromotion, as predicted in its cfg: the fail-open
\* defect (unperformable treated as pass) turns every formula that
\* speaks about unperformable checks red, not one. This cfg removes the
\* whole implicated set — StrictMeansEverything (red in _Broken),
\* NoSilentPromotion (red in _Green), and ValidNeedsNonWaivablePass
\* (red by reading: any VALID verdict with a non-waivable check
\* unperformable; masked in _Green because every such state also fails
\* NoSilentPromotion, which is listed first) — and checks the residue.
\* EXPECTED: GREEN — Partition, Monotonicity, and
\* DegradedNeedsExplicitWaiver do not depend on the missing branch;
\* UnverifiableIsHonest is VACUOUS here (the broken VerdictFor never
\* returns UNVERIFIABLE), which the committed reviews already note.
CONSTANTS
  Checks = {c1, c2, c3, c4, c5}
  NonWaivable = {c1, c2}

INIT Init
NEXT Next

INVARIANTS
  Partition
  Monotonicity
  DegradedNeedsExplicitWaiver
  UnverifiableIsHonest
