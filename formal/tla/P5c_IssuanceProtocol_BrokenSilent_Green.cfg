\* The isolation control: every invariant EXCEPT NoSilentDeadlock, on the
\* broken-silent module. EXPECTED: GREEN — establishing that the
\* _BrokenSilent break is red on exactly the deadlock invariant among the
\* checked set, and nothing else in it (Refuse's guard is honest, the
\* latch holds, ship-side invariants untouched).
CONSTANTS
  MaxTime = 14
  Delta = 3
  DepthK = 2
  MaxAttempts = 3

INIT Init
NEXT Next

INVARIANTS
  NoShippedOrphan
  ShippedIsSound
  ExpiredCannotShip
  RefusedOnlyWhenExhausted

PROPERTIES
  RefusalLatched
