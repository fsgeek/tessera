\* Isolation control for the receipt-NARROWED tolerances break (review
\* item 4, the P5c _Green pattern; added 2026-09-06 by the AI
\* collaborator, PROPOSED). The _BrokenTolStrict cfg's configured
\* invariants EXCEPT the expected-red ReceiptIndependence. EXPECTED:
\* GREEN — the three pure safety invariants cannot see the narrowing
\* bug (narrowed acceptance stays inside the policy and the maxima); the
\* committed red run stopped at its first violation and so certified
\* none of the three on its own. This green is the module's point made
\* mechanical: only ReceiptIndependence catches issuer-selected verdicts.
CONSTANTS
  MaxTime = 6
  DeltaMax = 3
  EpsilonMax = 1
  RcptTolMax = 6

INIT Init
NEXT Next

INVARIANTS
  ForgeryRejected
  WindowRespected
  VerifierOwnsTolerances
