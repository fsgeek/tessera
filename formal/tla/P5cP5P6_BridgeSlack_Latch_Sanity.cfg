\* Vacuity check: run with `tlc2.TLC -continue -deadlock`, piping through
\* `scripts/filter-tlc-output.sh`. VIOLATING both invariants is the
\* healthy result: shipping reachable under latch semantics, and the
\* PostEnvelopeShip state is live — shipped after the wall envelope on
\* timely eligibility, the exact state the execution-time guard of the
\* base module excluded (Codex constraint 1's scheduler artifact,
\* eliminated). ORDER: PostEnvelopeShip first (most specific;
\* first-fail-per-state cannot let ShipUnreachable mask it).
CONSTANTS
  MaxTime = 6
  Delta = 2
  Epsilon = 1
  KConf = 3
  DepthK = 2
  MaxSkew = 2
  MaxBlocks = 4
  Slack = 2

INIT Init
NEXT Next

INVARIANTS
  PostEnvelopeShipUnreachable
  ShipUnreachable
