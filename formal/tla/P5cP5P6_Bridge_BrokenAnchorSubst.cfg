\* EXPECTED (run with -continue): TWO invariants red — two faces of the
\* one substitution defect. ShippedDesignatedAgree (issuer evaluated
\* block h+DepthK, verifier reads block h) and LateBurialRejected (an
\* abandoned artifact with a late TRUE k-th confirmation but in-window
\* anchor timestamp is ACCEPTED — the burial delay vanishing in the
\* handoff). The two fire in disjoint states (shipped vs unshipped), so
\* neither masks the other. The _Green cfg proves the rest of the
\* invariant set survives.
CONSTANTS
  MaxTime = 6
  Delta = 2
  Epsilon = 1
  KConf = 3
  DepthK = 2
  MaxSkew = 2
  MaxBlocks = 4

INIT Init
NEXT Next

INVARIANTS
  ShippedDesignatedAgree
  LateBurialRejected
  PinAgreement
  HonestShipAccepted
