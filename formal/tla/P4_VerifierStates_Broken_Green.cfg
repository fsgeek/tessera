\* Isolation control for the P4 fail-open break (review item 4, the P5c
\* _Green pattern; added 2026-09-06 by the AI collaborator, PROPOSED).
\* The _Broken cfg's configured invariants EXCEPT the one that went red
\* in the committed _Broken run (StrictMeansEverything). If this run is
\* green, the break is isolated to that invariant among the checked set;
\* if it is red, the fail-open defect turns more than one invariant red
\* and the run reports which — that is a recorded result, not a hidden
\* one (see falsification-2026-09-06/FIXES-2026-09-06.md).
\* Collaborator prediction before running: RED on NoSilentPromotion
\* (c1 unperformable, waived = {} -> the broken VerdictFor returns
\* VALID_STRICT), because the fail-open defect is a multi-invariant
\* defect by nature, not a one-formula one.
CONSTANTS
  Checks = {c1, c2, c3, c4, c5}
  NonWaivable = {c1, c2}

INIT Init
NEXT Next

INVARIANTS
  Partition
  Monotonicity
  NoSilentPromotion
  ValidNeedsNonWaivablePass
  DegradedNeedsExplicitWaiver
  UnverifiableIsHonest
